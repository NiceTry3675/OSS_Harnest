# Harnest Background

> 이 문서는 `SPEC.md`와 `interview_schema.md`를 읽고, Harnest가 무엇을 만들려는 프로젝트인지 팀원이 같은 언어로 이해하기 위해 정리한 배경 문서다.

## 1. 한 문장으로

Harnest는 사용자가 웹에서 목표와 재료를 입력하면, 사용자가 승인한 평가 기준을 고정한 채 AI가 산출물을 반복 개선하도록 만드는 웹 서비스다.

핵심은 "AI에게 한 번 시키기"가 아니라 "어떤 기준으로 좋아졌다고 볼지 먼저 정하고, 그 기준으로 될 때까지 개선시키기"다.

## 2. 왜 필요한가

LLM과 에이전트는 이미 강하다. 문제는 대부분의 사용자가 좋은 결과를 얻기 위해 필요한 평가 구조를 직접 만들기 어렵다는 점이다.

예를 들어 자기소개서를 고치고 싶을 때 사용자는 보통 이렇게 말한다.

```txt
이 자소서 좀 고쳐줘.
```

그러면 AI는 한 번 고쳐줄 수는 있다. 하지만 다음 질문이 남는다.

- 무엇을 기준으로 좋아졌다고 판단할 것인가?
- 공고 핵심어가 얼마나 반영되었는가?
- 근거 없는 경험을 지어내지는 않았는가?
- 글자 수 제한을 지켰는가?
- 점수가 오르지 않은 수정은 버릴 수 있는가?
- 여러 번 시도하면서 더 나은 버전을 찾을 수 있는가?

개발자라면 평가 스크립트, 테스트 케이스, 실행 루프를 만들 수 있다. 하지만 비개발자는 이 구조를 만들기 어렵다. Harnest는 이 진입장벽을 웹 인터뷰와 템플릿으로 낮추려는 제품이다.

## 3. 하네스란 무엇인가

이 프로젝트에서 하네스는 단순한 프롬프트가 아니다.

하네스는 AI가 산출물을 개선할 때 따라야 하는 실행 구조다.

```txt
수정 대상
+ 평가 기준
+ 반복 설정
+ 채택/폐기 규칙
+ 실행 기록
= 개선 루프
```

Harnest가 만드는 하네스에는 최소한 다음이 포함된다.

- 수정 대상: 사용자가 개선하고 싶은 글, 일정, 프롬프트, 표 등
- 평가 기준: 사용자가 승인한 채점표
- 수정 가능 범위: AI가 고칠 수 있는 부분
- 수정 불가 범위: AI가 절대 바꾸면 안 되는 기준
- 반복 설정: 최대 반복 횟수, 목표 점수, 정체 종료 조건
- 실행 방식: 브라우저 Lite 실행 또는 개발자용 로컬 실행
- 결과 기록: 점수 변화, 채택/폐기 이력, diff

## 4. Harnest의 핵심 차별점

Harnest는 챗봇이 아니다.

챗봇은 사용자가 AI와 대화하면서 결과를 얻는다. Harnest는 사용자가 평가 기준을 승인하고, AI가 그 기준 안에서 실험하는 과정을 관찰하게 만든다.

Harnest의 차별점은 세 가지다.

1. 하네스 생성 진입로가 웹 인터뷰다.
2. 승인된 평가 기준은 AI가 바꿀 수 없다.
3. 비개발자도 실행을 웹에서 끝낼 수 있다.

즉, Harnest는 "더 좋은 답변을 생성하는 AI"라기보다 "더 좋아졌는지 확인할 수 있는 구조를 만들어주는 서비스"에 가깝다.

## 5. 타깃 사용자

### 5.1 주 타깃: 비개발자

주 사용자는 자소서, 시간표, 글쓰기, 상세페이지처럼 반복 개선이 필요한 결과물을 가진 일반 사용자다.

이 사용자는 다음을 원한다.

- 복잡한 CLI나 VS Code 없이 웹에서 끝내고 싶다.
- AI가 마음대로 기준을 바꾸지 않았으면 좋겠다.
- 결과가 왜 좋아졌는지 확인하고 싶다.
- 한 번 생성하고 끝이 아니라 여러 번 시도해서 더 나은 결과를 얻고 싶다.

### 5.2 부 타깃: 개발자

개발자는 고급 모드에서 하네스 zip을 받아 로컬 에이전트 CLI로 실행할 수 있다. 하지만 이 프로젝트의 본선 데모와 MVP는 비개발자 Lite 경로를 우선한다.

## 6. 사용자 흐름

Lite 기본 흐름은 다음과 같다.

```txt
템플릿 선택 또는 목표 입력
→ 인터뷰 위저드
→ 평가 기준 후보 확인
→ 사용자가 평가 기준 승인
→ 루프 설정 최종 확인
→ 브라우저에서 반복 실행
→ 결과와 개선 이력 확인
```

화면은 챗봇 말풍선이 아니라 폼과 관제실이어야 한다.

- 인터뷰 화면: 사용자가 답하면 오른쪽에 Live Blueprint가 조립된다.
- 기준 화면: 평가 기준과 가중치가 보이고 승인하면 잠긴다.
- 실행 화면: 실험 트리, 점수 곡선, diff, 채택/폐기가 보인다.
- 결과 화면: 최종 산출물, 시작/최종 점수, 개선 이력, 홀드아웃 점수가 보인다.

## 7. 데이터 흐름

`interview_schema.md`는 프론트와 생성 엔진 사이의 입력 계약이다.

중요한 점은 같은 스키마를 두 번 보낸다는 것이다.

### 7.1 1차 제출

사용자가 목표와 재료를 입력하면 프론트는 `evaluation: null`로 서버에 보낸다.

```txt
프론트
→ interview JSON, evaluation: null
→ 생성 엔진
→ 평가 기준 후보 응답
```

이 단계의 목적은 루프 스펙을 바로 만드는 것이 아니라, 사용자가 승인할 평가 기준 후보를 만드는 것이다.

### 7.2 최종 제출

사용자가 평가 기준을 승인하거나 수정하면 프론트는 승인된 `evaluation`을 포함해서 다시 보낸다.

```txt
프론트
→ interview JSON, evaluation: approved
→ 생성 엔진
→ loop spec 생성
→ 브라우저 실행 또는 zip 생성
```

이때 승인된 평가 기준은 불변이다.

## 8. 인터뷰 JSON에서 중요한 필드

### 8.1 `goal`

사용자의 자연어 목표다.

예:

```txt
카카오 서버 개발자 공고에 맞게 자기소개서를 개선하고 싶다
```

프론트에서는 이 값이 단순 제목이 아니라 생성 엔진이 평가 기준을 제안할 때 쓰는 핵심 입력이라고 이해해야 한다.

### 8.2 `artifact`

AI가 실제로 고칠 대상이다.

예:

```json
{
  "type": "text",
  "label": "자기소개서",
  "content": "저는 다양한 프로젝트를 경험했습니다...",
  "origin": "user"
}
```

`origin`이 `user`이면 사용자가 초안을 넣은 것이고, `generated`이면 초안 없이 재료만 넣고 엔진이 첫 버전을 만들어야 한다.

### 8.3 `answers`

템플릿 질문에 대한 답변 묶음이다.

질문 ID를 key로 쓰기 때문에 템플릿이 늘어나도 스키마 자체는 바뀌지 않는다.

예:

```json
{
  "job_posting": { "type": "paste", "value": "채용공고..." },
  "tone": { "type": "choice", "value": "정중하고 간결하게" },
  "length_limit": { "type": "number", "value": 1700 }
}
```

### 8.4 `evaluation`

사용자가 승인한 평가 기준이다. Harnest의 핵심이다.

1차 제출에서는 `null`이고, 최종 제출에서는 `approved` 상태가 된다.

여기에 들어가는 기준은 크게 두 종류다.

- deterministic: 키워드 포함, 글자 수, 제약 위반 수처럼 기계적으로 판단 가능한 기준
- llm_judge: 고정된 루브릭으로 LLM이 판단하는 기준

중요한 점은 평가 기준이 승인된 뒤에는 AI가 바꿀 수 없다는 것이다.

### 8.5 `loop`

반복 실행 설정이다.

예:

```json
{
  "maxIterations": 30,
  "llmRoute": "trial",
  "branching": { "width": 1 },
  "critic": true,
  "stop": { "targetScore": null, "plateauRounds": 8 }
}
```

프론트에서는 이 값을 단순 설정값이 아니라 실행 관제실 UI의 기반으로 써야 한다.

## 9. 평가 기준 동결이 중요한 이유

AI가 산출물도 고치고 평가 기준도 고칠 수 있으면 점수를 올리는 쉬운 방법은 기준을 바꾸는 것이다. 이것이 reward hacking 문제다.

Harnest는 이 문제를 막기 위해 역할을 분리한다.

```txt
Generator/Critic: 산출물을 개선할 수 있음
Judge: 사용자가 승인한 기준으로 고정됨
```

그래서 사용자에게는 "AI가 기준을 못 건드린다"는 신뢰 장치를 분명히 보여줘야 한다.

## 10. 루프 실행이란 무엇인가

루프 실행은 같은 산출물을 여러 번 고쳐보는 것이다.

한 라운드는 대략 다음처럼 동작한다.

```txt
현재 산출물
→ Generator가 수정 후보 생성
→ Judge가 후보 채점
→ 점수가 오르면 채택
→ 점수가 오르지 않으면 폐기
→ 반복
```

MVP에서는 실제 LLM 호출 없이도 mock으로 이 구조를 보여줄 수 있다.

하지만 화면은 처음부터 다음 데이터를 표현할 준비가 되어야 한다.

- 현재 점수
- 시작 대비 개선량
- 채택된 후보
- 폐기된 후보
- diff
- 반복 횟수
- 종료 조건

## 11. FastAPI가 맡을 최소 역할

팀에서 FastAPI를 쓰기로 했다면, E2E MVP에서는 다음 API를 먼저 만들면 된다.

```txt
POST /api/interviews/draft
  interview JSON(evaluation: null)을 받고 평가 기준 후보를 반환

POST /api/interviews/approved
  승인된 evaluation을 받고 loop spec을 반환

POST /api/runs
  loop spec 기반으로 mock 실행을 시작

GET /api/runs/{run_id}
  실행 상태, 점수, 이벤트, diff를 반환

POST /api/runs/{run_id}/result
  최종 결과 저장
```

처음에는 DB와 LLM 없이 결정적 scorer와 간단한 후보 생성기로 만들어도 된다. 중요한 것은 프론트가 실제 API 계약으로 동작하고, 이후 Generator/Critic/Judge 구조로 교체될 자리가 보이게 만드는 것이다.

현재 Lite API는 최종 AI 루프가 아니라 첫 vertical slice다.

```txt
현재 Lite API:
규칙 기반 후보 생성기
→ 결정적 scorer
→ 점수 상승 시 채택

목표 구조:
Generator N개
→ Judge가 승인된 기준으로 채점
→ 최고 후보 채택
→ 정체 시 Critic이 다음 변이 방향 제안
```

즉, 현재 구현은 "멀티에이전트가 완성됐다"가 아니라 "멀티에이전트가 들어갈 루프 계약과 화면 흐름을 먼저 관통했다"로 이해해야 한다.

## 12. 프론트엔드가 지금 이해해야 하는 것

프론트의 역할은 예쁜 화면을 만드는 것만이 아니다.

프론트는 사용자가 하네스를 이해하지 않아도 하네스를 만들 수 있게 도와야 한다.

그래서 프론트는 다음을 잘 보여줘야 한다.

- 내가 무엇을 개선하려고 하는가
- AI가 무엇을 고칠 수 있는가
- AI가 무엇을 고칠 수 없는가
- 어떤 기준으로 점수를 매기는가
- 기준별 가중치는 얼마인가
- 몇 번 반복할 것인가
- 언제 멈출 것인가
- 점수가 오른 수정은 무엇인가
- 폐기된 수정은 왜 버려졌는가

## 13. MVP 완료 기준

워킹 스켈레톤의 최소 완료 기준은 다음이다.

```txt
인터뷰 질문 3~4개 입력
→ 평가 기준 후보 생성
→ 사용자가 기준 승인
→ loop spec 생성
→ 브라우저에서 Lite 루프 실행
→ 점수 변화와 diff 표시
→ 결과 화면 표시
```

이 흐름이 한 번이라도 끝까지 돌아가면 팀원들이 각자 맡은 파트를 붙이기 쉬워진다.

## 14. 지금 프론트 작업의 의미

현재 프론트는 최종 제품이 아니라 다음을 검증하는 초기 뼈대다.

- 화면 흐름이 스펙의 사용자 흐름과 맞는가
- 인터뷰 JSON을 만들 수 있는가
- 평가 기준 승인 UI를 만들 수 있는가
- loop spec과 실행 결과를 표현할 수 있는가
- 백엔드 Lite API와 연결할 자리가 있는가

따라서 다음 단계는 디자인을 더 깎는 것보다 FastAPI Lite API와 연결해서 E2E 흐름을 만들고, 그 다음 Generator/Critic/Judge 멀티에이전트 구조로 확장하는 것이다.
