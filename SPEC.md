# Harnest 제품 명세

이 문서는 **현재 제품 정책과 안정 불변식**만 정의한다. 정확한 필드와 직렬화 형태는
`packages/contracts`의 타입과 테스트가 정본이다. 다른 문서의 역할은 `README.md`의 문서 지도를
따르며, 완료 이력은 Git에 맡긴다.

## 1. 제품 정의

Harnest는 사용자가 평가 절차를 확인하고 승인한 뒤, 그 절차를 실행 중 바꾸지 않은 채 산출물을
반복 개선하는 웹 관제실이다. 핵심 가치는 반복 횟수가 아니라 다음 세 가지다.

1. 무엇을 좋은 결과로 볼지 실행 전에 공개하고 승인한다.
2. 실험하는 쪽은 바뀔 수 있지만 승인된 판정 절차는 바뀌지 않는다.
3. 원샷과 최종 결과를 같은 기준으로 측정하고 홀드아웃 결과와 한계를 함께 보여준다.

## 2. 현재 범위

현재 주 경로는 브라우저에서 완결되는 웹 앱이다. FastAPI 서버는 사용자가 명시적으로 기록할 때만
프로젝트와 결과를 저장하는 선택 기능이며, 평가나 모델 호출을 대신 실행하지 않는다.

| 템플릿 | 용도 | 현재 평가 방식 |
|---|---|---|
| 인수인계·온보딩 문서 | 사용자 대상 플래그십 | 실제 질문·답 케이스 실측 + 간결성 가점(선택, 결정적) + 검증 가드 비퇴보 + 분량 hard gate + 시드 분할 홀드아웃 |
| 시간표 | 개발용 테스트베드 | 완전 결정적 채점, 모델 호출과 홀드아웃 없음 |

커스텀 endpoint, Claude 프리셋, pairwise 채택, 빔 탐색, 호스팅 인증·쿼터,
서버 측 홀드아웃 봉인, 고급 zip 실행기는 현재 지원 범위가 아니다. 향후 후보는 `ROADMAP.md`에서만 다룬다.

## 3. 안정 불변식

1. **데이터 경계** — BYO API 키와 Vertex 서비스 계정 자격 증명은 브라우저 `localStorage`에만
   두고 벤더 LLM 호출은 브라우저에서 직접 수행한다. Vertex private key는 JWT 서명에만 사용하며
   Google OAuth에는 서명된 assertion만 보낸다. 사용자가 선택한 프로젝트·결과 저장은 별도
   경로이며 벤더 자격 증명과 단기 access token을 포함하지 않는다.
2. **승인 증거** — LLM 판정이 있는 템플릿은 시험관 검증 리포트가 승인 전 요건이다.
   결정적 전용 템플릿은 §10에 따라 면제한다.
3. **사람의 통제** — 실행 전 평가 절차 승인은 필수다. 추가 검토와 실행 중 일시정지는 선택적으로
   제공할 수 있으며, 필수 개입 횟수를 임의의 숫자로 고정하지 않는다.
4. **동결 단위** — `definitionDigest`는 `packVersion`, `templateId`, `criteria`, `gates`,
   `judgeProcedure`, `holdoutPolicy` 전체를 정규화해 계산한다. 어느 필드든 바뀌면 재컴파일과 재승인이
   필요하다. hard gate는 가중 점수가 아니라 문이며, 현재 효과는 `reject`뿐이다. 인수인계 템플릿의
   사용자 분량값은 권장 상한이다. 상한부터 125%까지는 0~20점을 선형 감점하고, 125% 초과만
   비용·폭주 방지용 hard gate로 기각한다.
5. **채택 규칙** — 게이트 기각 후보는 채택 비교에 들어가지 않는다. 검증 가드가 구성된 팩에서는
   후보의 가드 집계가 챔피언 가드보다 허용 오차(`guardTolerance`) 넘게 낮으면 스칼라와 무관하게
   기각한다. 그 위에서 현재 모드는 후보의 동결 스칼라가 챔피언보다 **엄격히 높을 때만** 채택한다.
   동점은 기존 챔피언을 유지하므로 챔피언 곡선은 내려가지 않는다(가드 곡선은 허용 오차 안에서
   내려갈 수 있다).
6. **원샷 기준선** — 모든 실행은 라운드 0을 원샷 기준선으로 기록한다. 사용자 대상 비결정적 템플릿은
   적용 범위를 밝힌 실측 근거를 제시한다. 반복 측정은 신뢰도를 높이지만 단일 no-go가 템플릿을
   영구 금지하지는 않는다.
7. **홀드아웃·가드와 provenance** — 홀드아웃 케이스와 그 점수에서 파생된 신호는 생성·채택·중단에
   유입되지 않는다. 홀드아웃은 라운드 0과 종료 시에만 채점한다. 검증 가드는 매 라운드 채점하되
   **집계 점수만** 채택의 비퇴보 조건에 쓰고, 개별 질문·판정 사유는 Generator에 전달하지 않는다.
   실행 상태와 결과 감사에 필요한 사건만 기록하며 사용자의 열람은 기록하지 않는다.
8. **귀속과 경계** — 체크포인트는 자신을 만든 `definitionDigest`에만 귀속된다. 루프 엔진은 팩이나
   scorer를 수정할 경로를 갖지 않고, 템플릿이 제공한 함수만 호출한다.

## 4. 현재 사용자 흐름

1. 사용자가 템플릿을 고르고 필요한 원료·케이스·제약을 입력한다. 템플릿이 선언하면 원료를
   로컬 파일 첨부로 채울 수 있고(내용은 브라우저에서만 추출), 케이스 초안을 보조 호출로 받아
   확인 후 채택할 수 있다. 케이스 출처는 승인·결과 화면에 공개한다.
2. LLM 템플릿이면 저지 프로바이더와 모델을 선택하고 짧은 연결 테스트를 통과한다.
3. 브라우저가 Evaluation Pack과 루프 사양을 컴파일한다. 인수인계 템플릿은 이때 케이스 내용
   지문에서 유도한 시드로 케이스를 섞어 피드백 40% · 검증 가드 40% · 홀드아웃 20%(각 최소 1개)로
   분할한다. 케이스 초안이 자료 순서를 따르더라도 세 집합이 특정 주제군에 몰리지 않게 하는
   장치이며, 같은 시험지는 언제 컴파일해도 같은 분할이다.
4. 해당하는 경우 시험관 검증(승인 화면이 자동 실행)을 마친 뒤 사용자가 판정 절차를 승인한다.
5. 라운드 0을 채점하고, 가시 케이스의 판정 사유를 이용해 후보를 반복 생성·평가한다.
6. 최대 라운드, 정체 또는 척도 상한 도달 조건에서 종료하고 시작·종료 홀드아웃과 개선 기록을
   보여준다.

### 4.1 승인과 재승인

검증 리포트는 자기참조를 피하기 위해 현재 Evaluation Pack 필드가 아니라 별도 상태이며,
`forDigest`로 승인 대상에 결속된다. 팩을 다시 컴파일하면 이전 리포트를 삭제해 숨기지 않고
불일치로 무효화한 뒤, 새 다이제스트에 대한 검증을 자동으로 다시 실행한다(수정→재검증 왕복).

검증 `fail`은 승인을 차단한다 — 실패한 기준은 동결할 수 없고, 기준을 수정하면 새 다이제스트로
재검증된다. 네트워크·시간 초과·형식 오류 같은 운영 실패는 판정 결과가 아니며 언제든 재실행할
수 있다.

### 4.2 실행과 결과

실행은 승인 순간 캡처한 다이제스트와 같은 팩으로만 시작한다. 매 라운드와 상태 전이를 체크포인트로
저장하며, 일시정지는 진행 중인 라운드를 마친 뒤 성립한다. 결과 화면은 후보 점수와 챔피언 점수를
구분하고, 적용된 방어 장치와 면제 사유를 실제 상태대로 표시한다.

### 4.3 인터뷰 위저드

위저드는 템플릿이 선언한 질문을 렌더링하는 단계형 폼이다. 페이지는 템플릿별 조건문을 갖지 않고
`TemplateEntry`를 통해 질문·컴파일·실행·표시 기능을 조립한다.

## 5. 평가와 반복 실행

### 5.1 시험관 검증

시험관 배터리는 실행마다 달라질 수 있는 것, 즉 사용자가 고른 저지 모델만 검사한다.
인수인계 템플릿은 가시 케이스만 사용해 안정성(같은 문서 재채점의 흔들림)과 꼼수 내성
(날조·아첨 오염 응답의 0점 처리)을 검사하고 `pass`·`warn`·`fail`로 보고한다. 작은 표본에서
정밀한 백분율을 주장하지 않는다. 결정적 코드(게이트)는 배터리가 아니라 유닛 테스트가 증명하고,
설정의 산술적 성질(예: 분량 상한이 기록 전체 길이보다 넉넉해 베끼기 방어가 약한 경우)은
컴파일 시점의 정적 안내로 공개한다.

### 5.1.1 현재 채택 루프

- 라운드 0의 원샷 산출물을 최초 챔피언으로 둔다.
- Generator는 후보를 만들기 전에 템플릿이 정의한 수정 전략과 구체적인 실행 계획을 선언하고,
  이를 후보의 라운드 기록에 함께 남긴다. 최근 공개 실험 3개의 전략·점수 변화·판정 사유를 다음
  수정의 재료로 쓰며, 같은 전략이 그 기록 안에서 두 번 기각되면 다음 시도에서 선택할 수 없다.
  가드 기각 후보는 공개 점수가 좋아도 비공개 신호를 추론할 수 있으므로 공개 실험 기억과 반복
  실패 계산에서 통째로 제외한다.
- 케이스 responder는 산출물과 이번 채점 대상 질문 목록만 본다(배치 호출 1회, 질문마다 독립
  응답). 정답과 원자료는 responder 컨텍스트에 넣지 않으며, 피드백·가드·홀드아웃 채점은 서로
  같은 호출에 섞지 않는다. grader만 responder 답을 동결된 정답과 대조한다.
- 검증 가드는 매 라운드 별도 배치로 채점하되 집계 점수만 남긴다. 가드의 개별 질문·판정 사유는
  Generator 피드백(violations)에 싣지 않는다 — 가시 시험지 전용 과적합 문서가 채택되는 경로를
  막는 장치다. 허용 오차는 채점 반 단계(100 / (2 × 가드 케이스 수))를 소수 첫째 자리로 **올림**하여 팩에
  동결하고, 비교는 이진 표현 여유를 두고 수행한다 — 가드 점수도 소수 첫째 자리로 반올림되므로
  내림하거나 정확 비교하면 봐주기로 한 반 단계 하나가 등급에 따라 기각된다.
- 후보를 같은 동결 scorer로 평가한 뒤 gate 통과, 가드 비퇴보, 엄격 스칼라 개선을 모두 만족할
  때만 채택한다.
- 곡선에는 후보 점수가 아니라 채택 판정 후 챔피언 점수를 기록한다.
- 챔피언이 척도 상한 100점에 도달하면 즉시 종료한다(`ceiling`). 엄격 개선 규칙상 이후 채택이
  불가능하므로 남은 라운드는 결과에 영향을 줄 수 없는 비용이다. 그 외에는 연속 미채택이
  `plateauRounds`에 도달하거나 `maxRounds`를 채우면 종료한다.
- 한 실행의 비교 케이스 집합은 고정한다. 비용 때문에 부분집합을 도입한다면 실행 전에 선택해
  다이제스트 또는 manifest에 결속해야 하며 라운드마다 바꾸지 않는다.

### 5.2 비용 상한

BYO 키의 비용은 사용자 부담이므로, 폭주 경로를 제품 한도로 차단한다.

- **실행 1회 호출 예산** — 모델 호출이 있는 템플릿은 실행 1회(라운드 0, 루프, 홀드아웃 채점
  포함)의 호출 예산을 선언하고, 예산 소진 시 실행은 오류로 중단된다. 예산은 정상 실행이
  도달할 수 없는 백스톱으로 잡는다. 예산 초과는 판정 결과가 아니라 운영 한도이며 점수로
  변환하지 않고, 체크포인트는 보존된다.
- **검증 자동 실행의 폭주 방지** — 시험관 배터리는 같은 `definitionDigest`에 대해 자동으로는
  1회만 시작한다. 추가 실행(운영 실패 재시도 포함)은 사용자가 버튼으로 직접 시작한다.
- **케이스 수 상한** — 사용자 입력 케이스 수의 상한(과 그로부터 파생되는 피드백·가드·홀드아웃 수)은
  템플릿 질문 선언(`min`/`max`)이 정본이며, 컴파일이 같은 값을 재검증한다.

구체적 예산 수치는 각 템플릿이 소유한다(§6). 인터뷰 단계의 연결 테스트(제출당 1회)와
사용자가 버튼으로 직접 시작하는 보조 호출(예: 케이스 초안 — 클릭당 본 호출 1회, 형식
재시도 1회 이내)은 실행 1회 호출 예산에 포함하지 않는다. 보조 호출의 상한은 템플릿이
상수로 소유하며, 그 산출물은 사용자가 확인·수정한 뒤에만 제출에 포함된다.

### 5.3 확장 경계

pairwise, 빔 탐색, Critic, 실행 중 방향 수정은 현재 계약에 없다. 도입할 때는 기존
`scalar_strict` 의미와 홀드아웃 비유입을 깨지 않는 별도 계약과 테스트가 먼저 필요하다.

## 6. 템플릿 경계

템플릿은 질문, 컴파일러, scorer, Generator, 시험관, 산출물 표시를 선언한다. 엔진과 페이지는
이 인터페이스 안에서만 조립하며 템플릿별 분기를 추가하지 않는다.

공식 사용자 대상 LLM 템플릿은 원샷 기준선, 적용 가능한 방어 장치, 비용 상한, 범위를 밝힌 실측 근거를
제시한다. 개발용·교육용·실험용 템플릿은 목적과 한계를 표시하면 같은 출시 기준을 강제하지 않는다.
분량 gate의 밴드 같은 단일 실험의 휴리스틱은 해당 템플릿의 튜닝 지침이지 플랫폼 불변식이 아니다.

## 7. 저장과 프라이버시

브라우저 스냅샷은 팩, 검증 리포트, 승인 상태, 실행 식별자, 체크포인트와 홀드아웃
결과를 복원하되 벤더 자격 증명과 access token은 포함하지 않는다. 선택적 FastAPI 서버는 사용자가 기록을 요청한 프로젝트와
결과 JSON을 SQLite에 저장한다. 서버는 봉투 버전·고정 필드 구조·귀속 식별자를 확인하고 검색용 메타데이터를
추출할 수 있지만, 평가 의미를 실행·재계산·변형하거나 임의 코드를 실행하지 않는다.

따라서 프라이버시 약속은 “모든 프로젝트 데이터가 서버에 절대 가지 않는다”가 아니라 다음과 같다.

- BYO 자격 증명과 벤더 LLM 요청은 Harnest 서버를 거치지 않는다. Vertex 서비스 계정의 서명된
  assertion은 access token 발급을 위해 Google OAuth로 전송된다.
- 프로젝트와 결과는 기본적으로 브라우저에서 동작한다.
- 사용자가 기록을 요청하면 해당 프로젝트·결과 데이터는 저장 API로 전송될 수 있다.

## 8. 모델 경로

현재 지원 모델은 `README.md`에서 관리한다. 저지 프로바이더와 모델은 승인 전에
확정되고 다이제스트에 결속되며, 실행 모델이 승인 선언과 다르면 시작하지 않는다. 브라우저가 일부
인증 실패 응답의 상태와 본문을 CORS로 숨길 수 있으므로 저수준 `Failed to fetch`를 인증 실패로
단정하지 않는다. 실측 근거는 `experiments/byo-cors-openai/RESULT.md`에 있다.

## 9. 결과와 근거

결과는 원샷 기준선, 라운드별 후보·챔피언 기록, 종료 사유, 시작·종료 홀드아웃을 분리해 보여준다.
실험 수치에는 모델·데이터·실행 횟수의 범위를 함께 적고 단일 실행을 일반 법칙으로 표현하지 않는다.

## 10. 결정적 전용 특례

결정적 scorer만 사용하는 템플릿은 시험관 검증 리포트와 pairwise가 필요하지 않다.
화면에는 이를 숨기지 않고 “해당 없음”과 사유를 표시한다. 다이제스트 동결, strict scalar 채택,
체크포인트 귀속, 엔진·템플릿 경계는 동일하게 적용한다.
