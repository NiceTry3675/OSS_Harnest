# Harnest 기획 스펙

> 오픈소스 개발 대회(osscontest.kr) · 교내 경진대회 출품 프로젝트
> 버전 1.4 (2026-08-22)
> 설계 결정의 근거와 철학은 [PHILOSOPHY.md](PHILOSOPHY.md) 참조.

---

## 1. 개요

### 1.1 한 줄 정의
**Harnest** — 사용자가 웹 인터뷰에 답하면, 사용자가 승인한 평가 기준(불가침)으로 산출물을 될 때까지 스스로 고치는 **자율 개선 루프**를 만들어주는 웹 서비스.

### 1.2 컨셉 문장
- **제품(대외)**: "AI에게 한 번 시키고 끝이 아니라, 당신이 승인한 기준으로 될 때까지 스스로 고치는 AI"
- **기술(심사)**: "에이전트는 이미 강하다. 없는 건 믿을 수 있는 채점표다"

### 1.3 이름
**Harnest** = harness + nest. nest는 실행 환경이 아니라 "에이전트가 벗어날 수 없는 평가 규칙의 울타리"를 의미.

---

## 2. 문제 정의와 포지셔닝

### 2.1 배경
Karpathy의 autoresearch 구조(수정 가능 파일 / 불가침 평가 스크립트 / 행동 지침 / 실행 루프, 지표 개선 시 유지·아니면 롤백)는 강력하지만, **하네스 설계 자체가 진입장벽**이다. 특히 "내 문제의 성공 기준을 어떻게 기계적으로 판정할 것인가"(채점표 설계)가 병목.

### 2.2 포지셔닝
- 공식 에이전트 하네스(Claude Code 등)는 "루프를 돌리는 손". Harnest는 그 손이 소비할 **스펙(불가침 평가자 + 수정 스코프 + 행동 지침)을 생성하는 위 계층** — harness-agnostic, 경쟁이 아닌 보완재/스택 관계.
- 연구 계열(Meta-Harness, AutoAgent 등)이 "하네스 반복 개선"을 다룰 때, Harnest는 **하네스 제로 생성 + HITL + 대중 웹 서비스**로 제품화.

### 2.3 타깃: 비개발자 퍼스트
- 주 타깃: 인수인계·온보딩 문서, 가게 운영 매뉴얼, 강의 자료 등 "나 없이도 / 이 문서만으로"가 필요한 참조 문서를 다듬는 일반 사용자(§6 라인업과 정렬). 웹에서 인터뷰 → 웹에서 실행 → 결과 확인까지 완결.
- 부 타깃(고급 모드): 개발자. 하네스 스펙 zip을 받아 로컬 에이전트 CLI로 실행.
- 언어 원칙: 사용자 화면에서 하네스/autoresearch/hill-climbing 용어 금지. HITL은 "채점 기준은 당신이 승인하고, AI는 그걸 못 건드린다"는 신뢰 장치로 표현.

---

## 3. 핵심 설계 원칙

> 규범 정의의 단일 원본은 이 절이다 — PHILOSOPHY.md는 근거를, interview_schema.md는 인용을 담당하며 정의를 재서술하지 않는다. (문서 드리프트 방지, 2026-08-21)

1. **생성은 웹, 실행은 사용자 환경** — 서버는 생성(LLM 호출 + CRUD)만 담당. 실행은 비개발자=브라우저(Lite), 개발자=로컬(zip).
2. **HITL 두 지점만**:
   - 평가 기준 후보 제안 → **캘리브레이션 + 검증 리포트 확인** → 사용자 승인/수정. 캘리브레이션 = 사용자가 A/B 산출물 쌍(알려진 꼼수 예시 1개 이상 포함)을 직접 판정하고 시험관 판정과의 일치를 확인 — llm_judge 포함 루프 필수, 결정적 전용 면제(§10 특례). 검증 리포트는 통과/주의/실패로 표시.
   - 생성된 루프 설정 최종 검토 → 실행 전 승인
   - 승인된 평가자는 아키텍처 수준에서 동결 = reward hacking 방어. 두 지점은 필수 개입의 상한이지 통제의 상한이 아니다(pause & steer는 선택적 통제 — §5.3).
3. **템플릿 프리셋 + 커스텀 경로** — 폴더 1개 = 템플릿 1개, 커뮤니티 기여 가능 규격.
4. **평가 기준 원칙** — (A) 결정적 채점기, 또는 (B) 고정 루브릭 LLM-as-judge(동결) + 홀드아웃 케이스로 judge hacking 방어. 동결의 단위는 인원수가 아니라 **판정 절차 전체**: 채점 기준 전체(criteria — scorer·params·가중치 포함) + **hard gate** + 루브릭 + 패널 구성 + 집계 규칙 + **저지 모델 ID + 저지 프롬프트(버전) + 디코딩 파라미터**(case_answering의 responder 구성 포함) + pairwise 설정 + 홀드아웃 정책 (= 스키마 definitionDigest의 결속 범위). 스칼라 점수 집계(median/mean)와 pairwise 투표 집계(majority)는 별개 필드다. 내부 패널·투표는 허용, 루프에 반응한 변경은 금지. 일부 규칙은 가중치로 상쇄될 수 없다 — 날조, 절대 분량 초과, 필수 제약 위반은 **hard gate**(실격 또는 점수 상한)로 표현하며 문체 점수로 만회할 수 없다.
5. **방어 세트 기본 탑재** — 홀드아웃 / grounding 기준 / pairwise 판정 / provenance 기록은 옵션이 아니라 기본값. 단, pairwise는 llm_judge 포함 루프에 한하고(결정적 채점기만 쓰는 루프엔 비용만 추가), 그중에서도 케이스 실측 중심 루프는 pairwise 대신 제3 채택 모드를 따른다(§5.1.1 — 2026-08-22). grounding은 해당 조건에서 기본(`origin: "generated"` 등 — 인터뷰 스키마 §3). 결과 화면에 활성 방어를 명시.
6. **원샷 델타 시금석** — 모든 템플릿은 원샷 베이스라인(라운드 0) 대비 루프 개선 폭으로 자격을 심사. 델타가 없는 템플릿은 채택하지 않는다. 원샷 베이스라인은 사용자 대면 기준이다(카드에 베이스라인 모델명 표기). 심사·기술 서사용으로는 동일 예산 best-of-N 대비 비교(budget-matched delta)를 별도 실험 1회로 준비한다 — 제품 화면 지표가 아니라 Q&A 방어 카드.
7. **강제는 루프에게, 통제는 사용자에게** — 봉인·동결·불가침은 루프에 대한 강제. 사용자의 열람은 자유이며 기록되지 않고, 결과에 영향을 주는 쓰기(기준 수정, 홀드아웃 변경, 방향 개입)만 provenance로 기록. 케이스가 풍부한 도메인은 홀드아웃 사용자 지정 지원(승인 시점에 지정 = HITL 지점 불변). 홀드아웃 불변식은 둘 — Generator/Critic 컨텍스트 미포함 + **홀드아웃 점수에서 파생된 신호의 생성·채택·중단 유입 금지** — 이며, 이를 구조로 보장하기 위해 홀드아웃 채점은 라운드 0과 종료 시에만 수행한다. 홀드아웃 케이스의 수정·가시 세트 이동은 판정 절차의 변경 = **재승인**(2026-08-21 결정 — 종전 재분할/배지 반납 방식 폐기).

---

## 4. 제품 정의

### 4.1 사용자 흐름 (Lite 기본 경로)
1. 템플릿 선택 (예: 인수인계 문서)
2. 인터뷰 위저드 — 답할 때마다 우측에 설계도가 실시간 조립 (라이브 블루프린트)
3. 평가 기준 승인 — **캘리브레이션**(A/B 예시 쌍 + 꼼수 예시를 직접 판정, 시험관 판정과의 일치 확인)과 **시험관 검증 리포트**(순서·변별력·안정성·꼼수 내성 — 통과/주의/실패)를 확인·수정 후 승인. 승인 순간 **Evaluation Pack**으로 동결(잠금 연출). 기준을 수정하면 리포트가 무효화되므로 수정→재검증 왕복 UX를 함께 설계
4. 루프 실행 — 라운드 0으로 원샷 베이스라인을 채점해 기준선으로 깔고, 브라우저에서 실험 반복. 관제실 화면(실험 트리 + 개선 곡선 + diff)으로 관찰
5. 결과 — 최종 산출물 + 개선 이력(원샷 대비 델타, 베이스라인 모델명 표기) + 홀드아웃 점수(라운드 0·종료 시 채점) + 활성 방어 세트 표시 + 공유 카드

### 4.2 실행 모드
| | Lite (기본) | 고급 모드 |
|---|---|---|
| 대상 | 비개발자, 텍스트 전용 도메인 | 개발자, 코드 실행 필요 도메인 |
| 실행 위치 | 브라우저 (루프 엔진 JS) | 로컬 (zip: 수정 대상 / 평가 스크립트 / program.md / run.sh) |
| LLM 호출 | 무료 체험: 서버 프록시(쿼터 · 체험 모델 `gemini-3.7-flash` — §12 미결 2) / 이후: BYO API 키 브라우저 직행(기본 4종 프리셋 + 커스텀 프로바이더 직접 입력 — §12 미결 2·7) | 사용자 로컬 환경의 에이전트 CLI |
| 결과 | 자동 기록·대시보드 | 결과 업로드 API로 대시보드 연동 |

하이브리드 원칙: **루프 엔진은 한 벌(브라우저)**, LLM 호출 경로만 스위치. BYO 경로는 키·민감 텍스트가 서버를 거치지 않음(프라이버시).

고급 모드 zip = **Evaluation Pack + 실행 스크립트의 포장** — 팩과 출력 명세를 공유해 명세 표면적을 최소화한다. 우선순위는 최하위(핵심 기능·부가 기능 3종 이후): 범용성의 증거로 유지하되 v1 데모의 경로는 아니다. (2026-08-21)

### 4.3 UX 아이덴티티: "챗봇이 아니라 관제실"
- 사용자는 AI와 대화하지 않고, AI가 실험하는 것을 **지켜보고 통제**한다.
- 실행 화면: 실험 트리(git 그래프 감성 — 채택 가지만 본선으로, 폐기 가지는 회색), 점수·개선 곡선 라이브 차트, 채택 시점별 diff 하이라이트.
- 위저드: 채팅 UI 금지. 폼 + 라이브 블루프린트.
- 불가침 연출: 평가 기준 승인 시 잠금(자물쇠) 애니메이션.

---

## 5. 기능 명세

### 5.1 핵심 기능
| 기능 | 설명 | 담당 파트 |
|---|---|---|
| 인터뷰 위저드 | 템플릿별 질문 세트, 라이브 블루프린트, 평가 기준 승인 UI | 프론트 |
| 생성 엔진 | 인터뷰 답변(JSON) → LLM 파이프라인 → **Evaluation Pack 컴파일**(기준+gates·판정 절차·홀드아웃 정책·검증 리포트·다이제스트) + 캘리브레이션 예시 생성 + 시험관 검증 배터리(순서·변별력·안정성·꼼수 내성 — 통과/주의/실패) + 루프 스펙/zip 생성 | PM |
| 브라우저 루프 엔진 | 원샷 베이스라인(라운드 0) 채점, hill-climbing 실행(§5.1.1), 채점기 JS, LLM 호출 경로 스위치, 키 관리. **첫 커밋부터 체크포인트·재개 가능한 상태 머신(IndexedDB)** — 백그라운드 탭 스로틀링·회수 대응, 체크포인트 로그가 곧 리플레이 데이터. **프롬프트 캐싱 필수**(최대 비용 레버) | 프론트 |
| LLM 프록시 | 무료 체험용. 로그인 필수, 실행 쿼터 + 반복 상한 + 모델 제한(체험 모델 `gemini-3.7-flash`) | 백엔드 |
| 결과 대시보드 | 실험 횟수, 채택/폐기 이력, 개선 곡선(원샷 베이스라인 기준선), 활성 방어 세트 표시, 로컬 실행 결과 업로드 | 프론트+백엔드 |
| 웹 백엔드 | 프로젝트 CRUD, 인증, 다운로드/공유, 결과 업로드 API | 백엔드 |

### 5.1.1 루프 구조와 채택 규칙

**v1은 의도적으로 단순하다** — 평가자 유도 루프가 단순한 형태로 먼저 테제를 증명해야 개선의 인과가 깨끗하게 읽힌다(2026-08-21). 빔 병렬 탐색·Critic은 §5.3 스트레치로 이동.

- **v1 루프**: 라운드당 Generator 1개가 변이 제안 → 채점 → 채택/롤백. 정체(plateau) 감지 시 조기 종료(`plateauRounds`). 실험 트리는 빔 1에서도 채택/폐기 가지로 성립.
- **케이스 트레이스는 v1부터 Generator의 입력이다** (2026-08-22 승격): evaluator가 만든 케이스별 구조화 오류(예: "case-5: 환불 절차 누락", "case-8: hard gate 실패 — 근거 없는 일정 약속")를 다음 라운드 Generator에게 그대로 전달한다. "점수만 피드백하면 개선 없음, 트레이스 공유 필요"(§5.3 Critic 근거)의 최소 구현이며, 별도 진단 에이전트는 필요 없다. **트레이스는 가시 케이스에서만 나온다** — 홀드아웃은 라운드 0·종료 시에만 채점되므로(§3 원칙 7) 루프 중 홀드아웃 트레이스는 존재 자체가 불가능하다.
- **채택은 pairwise, 서사는 스칼라**: 채택/롤백 판정은 챔피언 vs 후보의 pairwise 비교로 — 절대 점수보다 판정 신뢰도가 높다. 개선 곡선·홀드아웃 서사는 동결 루브릭의 스칼라 점수가 담당.
- **채택 규칙 3종** (동결 대상 — Evaluation Pack 명세에 명문화, §12 미결 4):
  1. 채택은 위치를 바꾼 **양방향 pairwise 모두 승리**일 때만. 순서에 따라 판정이 갈리면(불일치) 챔피언 유지.
  2. 곡선에 기록되는 스칼라는 라운드에서 이긴 점수가 아니라 **채택 확정 후 챔피언의 신규 재채점** — 후보들의 최고점을 그대로 찍으면 노이즈만으로 곡선이 오른다(winner's curse).
  3. pairwise 채택과 스칼라 곡선은 어긋날 수 있다 — **곡선 하락은 그대로 표시**하고 보정하지 않는다.
  - 장황함 보정은 저지 프롬프트에 숨은 플래그가 아니라 **결정적 규칙**(명시된 길이 보정 공식)으로 (인터뷰 스키마 v0.3 `lengthCorrection`).
  - 결정적 채점 전용 루프(pairwise 금지)의 채택 규칙: **스칼라가 엄격히 개선될 때만 채택**(동점은 챔피언 유지) — 스켈레톤이 쓰는 규칙(§10 특례).
  - **케이스 실측 중심 루프의 채택 규칙(제3 모드, 2026-08-22 신설)**: 채점이 case_answering(→v0.4 case_execution) 집계 중심인 루프는 pairwise를 적용하지 않고 **동결 가중 총점의 엄격 개선 + gate 통과**로 채택한다(동점은 챔피언 유지). 근거: pairwise가 비교해야 할 것은 문서 텍스트가 아니라 케이스 성능인데, 저지의 텍스트 직접 비교는 케이스 성능을 모르고, 케이스 출력 전수 pairwise는 비용이 폭발한다. 케이스 집계 스칼라는 절대 점수 노이즈(pairwise의 원래 존재 이유)가 구조적으로 작다. 발동 조건의 정확한 경계(케이스 실측 가중치 기준 등)는 미결 4에서 Evaluation Pack 명세와 함께 확정.
- **불가침 경계**: 실험하는 쪽(Generator — 스트레치의 빔·Critic 포함)은 자유롭게 멀티에이전트로 확장 가능. 채점하는 쪽(Judge)도 내부적으로 패널·투표 구성은 가능하나, **판정 절차 전체(§3 원칙 4의 필드 목록)가 승인 시점에 동결** — 루프에 반응해 변할 수 없다. 이 비대칭(실험은 가변, 판정은 불변)이 Harnest의 정체성.

### 5.2 채택 확정 부가 기능 (우선순위순)
1. **리플레이 모드** — 실행 기록 저장 → 타임라인 재생. 발표·심사 라이브 리스크 제거, 공유 기능의 기반.
2. **홀드아웃 점수 공개** — 루프가 보지 못한(agent-blind) 홀드아웃 케이스 점수를 결과 화면에 표시("본 적 없는 기준에서도 +N점" — 주어는 루프). 채점은 라운드 0과 종료 시에만(§3 원칙 7). judge hacking 방어를 신뢰 서사로 전환. (홀드아웃 채점 자체는 §3 원칙 5의 기본값 — 본 항목은 그 결과의 표시 UI만을 가리킴.) 카드·결과 화면에 홀드아웃 provenance(자동 분할 vs 사용자 지정) 표기.
3. **공유 카드** — 개선 곡선(예: 62→78점) 이미지/링크 내보내기 + **베이스라인·구동 모델명 표기**(§3 원칙 6). 유입 루프 + 정량 지표. 링크 프리뷰(OG 태그)는 백엔드 공유 엔드포인트에서 처리(§7 참조).

### 5.3 스트레치 골 (핵심 기능 완료 후 여유 시 추가)
- **루프 중간 개입(pause & steer)** — 루프를 멈추고 방향 힌트를 준 뒤 재개. 관제실 UX("지켜보고 통제한다")의 자연스러운 연장이라 여유 시 1순위. HITL 두 지점 원칙과 모순 아님 — 두 지점은 필수 개입의 상한이지 통제의 상한이 아니다(§3 원칙 2). 개입은 쓰기이므로 provenance 기록. 단, 워킹 스켈레톤·부가 기능 3종보다 후순위.
- **빔 병렬 탐색** — 라운드당 Generator N개가 서로 다른 방향의 변이를 동시 제안 → 전부 채점 → 최고만 채택. 국소 최적점 탈출 + 실험 트리가 실제로 갈라지는 시각 임팩트. BYO 슬라이더(1~4), 무료 체험은 1 고정 유지.
- **Critic 에이전트** — 정체 시 채점 내역·트레이스를 읽고 다음 변이 방향을 진단해 Generator에 전달. 근거: "점수만 피드백하면 개선 없음, 트레이스 공유 필요"라는 선행 연구 발견의 제품 번역. 트레이스 자체의 전달은 v1로 승격됐으므로(§5.1.1, 2026-08-22), Critic의 남은 몫은 트레이스를 넘어 "다음 변이 방향"을 진단하는 상위 계층. v1은 정체 감지 + 조기 종료 + 트레이스 전달까지.

### 5.4 로드맵 (대회 범위 제외)
- "스킬 최적화" 템플릿(SKILL.md 대상), GPU 필요 도메인(소형 ML 훈련 등)

---

## 6. 템플릿 라인업

모든 템플릿은 두 시금석을 통과해야 한다 — **원샷 델타**(원샷 베이스라인 대비 루프 개선 폭이 실증되는가)와 **기준의 저자**(평가 기준을 사용자만 쓸 수 있는가). 근거: PHILOSOPHY.md §2·§3.

### 비개발자 (플래그십 1 + 후속 — 2026-08-22 재편)
| 템플릿 | 평가 방식 | 상태 |
|---|---|---|
| 인수인계·온보딩 문서 ("나 없이도 돌아가는 문서") | 케이스=실제로 받았던 질문·요청 로그, 정답=당시의 실제 답변·처리 — case_answering(스키마 §5, v0.4에서 case_execution으로 일반화 예정) + 분량 hard gate + grounding | **플래그십(확정 — 실측 go, 2026-08-22)** — 원샷 델타 실측 통과(§12 미결 6, experiments/delta-01/report2.md): 체험 티어 구성에서 **루프만이 분량 게이트를 지키는 유효 문서를 생성**(가시 77.1·홀드아웃 23.8 vs 원샷·반복 첨삭·best-of-10 전원 실격). 서사: 원샷은 "저자가 중요하다고 생각하는 것"을 쓰고 로그는 "사람들이 실제로 묻는 것"을 드러낸다 — 그리고 제약이 실제로 물리는 문제에서 원샷은 제약 자체를 못 지킨다. 민감 로그 = BYO 프라이버시의 실질 셀링 포인트 |
| OSS 기여자 온보딩 문서 | 케이스=공개 저장소의 실제 이슈·디스커션 질문, 정답=메인테이너의 실제 답변 | **대회 데모(도그푸딩)** — Harnest 저장소 자신의 온보딩 문서를 Harnest로 생성해 제출. 공개 데이터라 심사위원이 같은 Evaluation Pack으로 재현 가능. 데모의 주인공은 F1이 아니라 인터뷰→캘리브레이션→동결→홀드아웃 |
| 가게 운영 매뉴얼 (알바 온보딩) | 케이스=직원·고객의 실제 문의 로그 | **스킨 1** — "신입 알바가 이 매뉴얼만 보고 첫 주를 버티는가". "봇 지침으로 내보내기"는 조용한 부가 기능 — 정체성은 사람이 읽는 문서에 남긴다 |
| 강사·과외 개념 정리 프린트 | 케이스=연습문제·기출(정답 있음) | **스킨 2** — 학기·학생 단위로 반복 사용. 시험 요약본을 학생에서 선생으로 뒤집은 것 — 일회성 소비 약점 해소, 엔진 무변경 |
| 시험 요약본/치트시트 | 케이스=기출 | **보조(2026-08-22 강등)** — 산출물이 시험 1회로 소비되어 반복 적용 서사가 약하고 계절성이 있다. 강사 프린트의 학생판으로 유지 |
| 자기소개서 ↔ 채용공고 매칭 | 키워드 커버리지(결정적) + 고정 루브릭 | **보류(2026-08-21)** — 델타 실측 대기. 단일 문서×단일 공고라 케이스 반복 구조가 아니고, "좋음"의 저자가 사용자가 아니라 채용담당자. 그라운딩 시연용으로 후일 재검토 |
| 시간표/근무표 짜기 | 제약 위반 개수(완전 결정적) | **내부 테스트베드(2026-08-21)** — 사용자 라인업 제외. 스켈레톤 고정 템플릿(§10). PHILOSOPHY §6("결정적 규칙 문제의 솔버가 되지 않는다")과 정합 |

**"검증된 압축" 가족**: 제약(분량) 아래에서 실제 질문에 답하는 능력을 보존하는 참조 문서 — 인수인계·온보딩 문서·가게 매뉴얼·강사 프린트·시험 요약본은 같은 엔진의 스킨이다. 서사는 가족으로 말하고, 구현은 인수인계·온보딩 하나부터 한다. 플래그십 선정 렌즈 2종 추가(2026-08-22, PHILOSOPHY §4): ① **반복 소비** — 산출물이 계속 새 케이스를 만나야 홀드아웃 점수가 "미래 성능의 예측"이라는 실질 가치가 된다. ② **산출물의 독자는 사람** — AI 지침(플레이북·프롬프트)을 플래그십으로 올리는 것은 §9의 skill-creator 경계("산출물도 스킬이지 사용자의 문서가 아님")를 스스로 지우는 일이라 채택하지 않는다. AI 지침 최적화는 고급 모드(프롬프트 최적화)의 몫이고, Lite에서는 내보내기 부가 기능까지만.

예비: 설문지 설계, 면접 질문 세트, 개인 문체 가이드, 이메일 템플릿, 퀴즈/플래시카드. (설문지·면접 질문·문체 가이드는 정답 로그가 없어 응답자 시뮬레이션의 순환(PHILOSOPHY §8이 경계하는 "생성기가 상상한 케이스") 리스크 — 후순위 사유. 기존 후보였던 SEO·유튜브 제목은 보편 기준 도메인이라 버티컬 비교에 노출 — 후순위.)

### 개발자 (고급 모드 1~2개)
- **프롬프트 최적화** (확정, 개발자 데모 겸용) / 예비: SQL 튜닝, 정규식·파서, 코드 성능.

### 템플릿 규격 (커뮤니티 기여 단위)
폴더 1개 = 질문 세트 + 채점기 + 루브릭 + 예제 + 홀드아웃 평가 케이스 + **꼼수 라이브러리**(이 도메인에서 관찰된 reward hack과 그 방어 — 시험관 검증·캘리브레이션의 프로브로 사용. 실행 관찰이 쌓이기 전에도 문헌으로 시딩해 데모 데이부터 비어 있지 않게: 장황함 인플레, 통째 베끼기, 자신 있는 날조, 아첨 등). 코드 없이도 기여 가능하게 설계.

경계 원칙: 템플릿은 신뢰 가능한 부품의 **범위 선언**(채점기 후보, 질문, 꼼수 라이브러리, 홀드아웃 생성 규칙), 엔진은 그 범위 안의 사용자 맞춤 **조합·파라미터화 + 검증**. "프리셋이 아니라 생성"이라는 방어 논리의 실체가 이 선이다. 템플릿은 **사용자 지정 홀드아웃 지원 여부**도 선언한다 — 케이스가 풍부한 도메인(질문 로그·기출 등)은 지원, 엔진이 케이스를 합성하는 도메인은 자동 분할만. **가시 케이스 상한**도 템플릿 규격의 선언 필드다(2026-08-22) — 케이스 실측 채점의 비용 지배 변수이며, 라운드별 케이스 샘플링 금지 원칙(곡선 파손) 아래에서 비용을 제어하는 유일한 손잡이(§12 미결 2). **실측 교훈 3건(2026-08-22, 델타 01·02에서 실증 — 규격 요건으로 승격)**: ① 분량 hard gate는 원샷 자연 길이를 실측한 뒤 그보다 작게 설정한다 — 게이트가 안 물리면 압축 문제 자체가 성립하지 않는다(델타 01). ② hard gate에는 생성 지침용 여유 목표(예: 상한의 80% + 단어 수 앵커)를 함께 내려보낸다 — 문자 수 상한은 지시만으로 준수 불가한 모델 한계(델타 02a). ③ 홀드아웃 분할은 질문 반복을 보존한다 — 반복이 이 가족의 측정 대상 그 자체다(델타 02).

---

## 7. 아키텍처 요약

```
[인터뷰 위저드] --인터뷰 JSON--> [생성 엔진(서버)] --Evaluation Pack·루프 스펙--> ┬ Lite: [브라우저 루프 엔진]
                                                              └ 고급: [zip 다운로드 → 로컬 실행]
[브라우저 루프 엔진] --LLM 호출--> 무료: 서버 프록시 / BYO: LLM API 직행
[실행 결과] --> [결과 대시보드(웹)]  (Lite 자동 / 로컬은 업로드)
```

- 서버는 임의 코드 실행 없음(생성 + 프록시 + CRUD만) → 저비용 지속 운영.
- 스택(확정): React+Vite+TS (SPA) / FastAPI + PostgreSQL / LLM API / Vercel+클라우드 / GitHub Actions / MIT or Apache 2.0.
  - SPA 선택 근거: 앱 핵심이 클라이언트 중심(브라우저 루프 엔진, 관제실, BYO 키 직행)이고 백엔드가 별도 계층이라 SSR 이점 없음. 루프 엔진 npm 패키지 분리(§8)와도 모노레포 구성이 자연스러움.
  - 단, 공유 링크의 OG 메타태그(링크 프리뷰)는 SPA가 처리 불가 → FastAPI가 공유 전용 엔드포인트에서 OG 태그 포함 HTML 응답 후 앱으로 리다이렉트 (백엔드 담당).
- 기술 검증 필요: BYO 직행용 LLM API의 브라우저 CORS 허용 여부 벤더별 확인.

---

## 8. 오픈소스 전략 (양방향)

- **받는 기여**: 폴더 단위 템플릿 규격 전면화 — 코드 못 짜는 사람도 기여하는 오픈소스. 템플릿 갤러리, 제출 템플릿 자동 검증 CI, good first issue 설계.
- **하는 기여**: 부품 2개를 독립 패키지로 분리 배포 — (1) 브라우저 hill-climbing 루프 엔진(npm), (2) 결정적 채점기 모음(키워드 커버리지, 제약 검사, 가독성 등).
- 서사: "Harnest는 서비스이자, 부품들이 각각 재사용 가능한 오픈소스 라이브러리."

---

## 9. 경쟁/차별 논리 (심사 대응) — 2026-08-21 재작성

한 줄 포지셔닝: **"모두가 autoresearch의 루프를 베꼈다. 아무도 평가자를 생성하지 않는다."** — Karpathy autoresearch의 후속 프로젝트들(autoexp, pi-autoresearch, AutoKernel 등)은 하나같이 "평가자는 이미 존재한다"를 명시적 전제로 시작한다. Harnest는 그 전제를 만드는 계층이다.

| 비교 대상 | 그들 | Harnest |
|---|---|---|
| 공식 하네스 (Claude Code 등) | 루프를 돌리는 "손". 평가자 분리는 사용자가 지켜야 하는 규율 | 손이 소비할 "채점표"를 검증까지 마쳐 생성, 동결이 유일한 경로 — 스택 관계 |
| Anthropic skill-creator 계열 | 자연어에서 eval 자동 생성 + 블라인드 A/B까지 도달(비개발자 SME 대상) — "그들은 못 한다"는 서술은 낡았다 | 남은 경계로 승부: 동결 없음·홀드아웃 없음·저지 검증(캘리브레이션) 없음·자율 루프 없음, 산출물도 스킬이지 사용자의 문서가 아님 |
| 연구 계열 (Meta-Harness 등) | 하네스 반복 개선 연구 | 하네스 제로 생성 + HITL 제품 |
| DSPy GEPA / 프롬프트 옵티마이저 | 주어진 지표를 최적화(트레이스 판독 포함). "GEPA에 UI 씌운 것 아니냐"가 예상 질문 | GEPA가 요구하는 전제(지표 + 데이터셋)를 제조·검증하는 계층 — 지표를 최적화하는 게 아니라 지표를 만든다 |

방어 논리: 동결·홀드아웃 같은 개별 패턴은 어떤 하네스든 흉내 낼 수 있다 — 차별점은 기능이 아니라 (a) 동결할 가치가 있는 시험관을 캘리브레이션·검증까지 마쳐 뽑아주는 생성 엔진, (b) 방어 세트(홀드아웃·grounding·pairwise·provenance)의 기본 탑재, (c) **표준 선점과 생태계** — 꼼수 라이브러리·템플릿 규격은 오픈소스로 공개되므로 "파일을 못 베낀다"가 아니라 "Evaluation Pack 포맷·검증 규격·기여 모멘텀이 표준이 되는 속도를 못 베낀다"가 정직한 주장이다(2026-08-21 문구 교체). 공식 진영이 eval 자동 생성을 내장해도 (d) 비개발자의 자기 산출물 도메인은 그들이 안 가는 영역, (e) 웹 인터뷰 진입로는 CLI와 다른 시장. 루프 자체는 방어선이 아니다 — 얇은 클론은 언제든 나올 수 있고, 데모의 주인공은 루프가 아니라 검증된 시험관과 홀드아웃 증거다.

---

## 10. 팀 구성과 운영

| 역할 | 인원 | 담당 |
|---|---|---|
| PM + 생성 엔진 | 1 (준현) | 총괄, 인터뷰 JSON 스키마·생성물 구조 확정 |
| 프론트 | 2 | 인터뷰 위저드 + 브라우저 루프 엔진 + 관제실 대시보드 |
| 백엔드 | 1~2 | CRUD/인증 + LLM 프록시 + 결과 업로드 API |
| 템플릿 팩 | 1~2 | 도메인 owner 로테이션 (전원이 제품 전체 흐름 경험) |

운영 장치: **워킹 스켈레톤 우선** — 1~2주 내 "인터뷰(질문 3개) → 생성 → Lite 루프 1회전 → 결과 표시"를 조잡하게라도 전원이 함께 관통. 스켈레톤 템플릿은 **시간표**로 고정: 채점이 완전 결정적이라 LLM 저지 변수 없이 파이프라인을 디버깅할 수 있다 (제품 데모 순위와 별개인 테스트베드 가치 — §6).

**결정적 전용 루프의 특례 2건 (2026-08-21)**: ① 시험관 검증 리포트·캘리브레이션은 llm_judge 포함 루프의 요건 — 결정적 전용은 면제. 근거: 캘리브레이션할 저지가 없고, 결정적 채점기의 올바름은 저지 검증이 아니라 코드·파라미터 검토의 문제다(순서·변별력 sanity 검사와 면제 루프의 방어 세트 표기 규칙은 미결 4 백로그). ② 채택 규칙은 pairwise가 아니라 "스칼라 엄격 개선 시 채택"(§5.1.1). 이 특례로 스켈레톤이 규칙 위반 없이 성립한다.

루프 엔진은 첫 커밋부터 체크포인트 상태 머신(§5.1). 계약 문서 3종(인터뷰 JSON 스키마 / **Evaluation Pack·루프 스펙 명세**(zip 포함) / 스켈레톤 DoD)을 먼저 확정. 프론트 투자보다 선행하는 실측 2건: 온보딩 문서 원샷 델타 — **완료(go, 2026-08-22, §12 미결 6)**, BYO CORS(§12 미결 3 — 잔여).

---

## 11. 성과 계획

1. 공개SW 오픈소스 개발자대회(osscontest.kr) 출품 (확정)
2. 교내 경진대회 발표 (확정)
3. 정식 서비스 배포 + 피드백 기반 개선 1회 이상
4. 오픈소스 커뮤니티: README·기여 가이드·템플릿 규격 완비, 외부 기여 유치, 부품 라이브러리 2종 배포
5. 기술 확산: 기술 블로그, 개발자 커뮤니티 공유
6. 정량 지표: 하네스 생성 건수, Lite 실행 수, 템플릿별 분포, 결과 업로드율, 공유 카드 유입

---

## 12. 미결 사항 (우선순위순)

1. 인터뷰 JSON 스키마 — **v0.3 완료** (2026-08-21): hard gate(`gates`), 저지 동결 필드(모델·프롬프트 버전·디코딩, 스칼라/pairwise 집계 분리), examinerReport 다이제스트 범위를 판정 절차 전체로 확장, 캘리브레이션 요건, caseList 확장 필드(expectedAnswer·evidenceRefs), 결정적 전용 면제. 케이스 기반 채점은 **해결(2026-08-22)**: `case_answering` kind + `judge.responder`(스키마 §5) — responder가 산출물만 보고 케이스를 실측으로 풀고 grader가 정답 대조, responder 컨텍스트 불변식 포함. 남은 것: 1차 제출 응답 스키마(기준 후보 + 검증 리포트 본문 + 캘리브레이션 결과·통과 규칙), 정식 interview.schema.json 파일. **v0.4 백로그 추가(2026-08-22, GPT_PROP 평가 반영)**: `case_answering`→`case_execution`·`judge.responder`→`executor` 개명(의미 일반화 — "산출물을 규칙·참조로 사용해 케이스를 처리". 요약본×기출, 매뉴얼×문의, 문체 가이드×작성 요청이 같은 kind. 불변식·판정식은 그대로 이식), `expectedAnswer` 문자열 → 구조화 `expected`(필수 포함 `mustMention`·금지 주장 `mustNotClaim` 등 — grader LLM 콜의 상당 부분을 결정적 검사로 대체), 원시 로그 붙여넣기 → 케이스(질문·정답 쌍) 추출·사용자 확인 위저드 플로우(캘리브레이션의 "사용자 판단 = ground truth 공급"과 자연 결합).
2. 무료 티어 정책 — **수치 결정** (2026-08-21): **10회 반복 · 빔 1 · 계정당 실행 2~3회 · 최저가 모델 · 프롬프트 캐싱 필수**(입력 토큰 50~80% 절감 — 최대 비용 레버). 30회 반복은 BYO 옵트인. **모델 정책 확정(2026-08-22)**: 체험 = `gemini-3.7-flash` — 컨텍스트 캐싱 지원(캐시 읽기 ~90% 할인)으로 "프롬프트 캐싱 필수" 전제 충족. 주의: 현 단가($0.75/M 입력)는 도입가로 **2027-01-01부 2배 인상 예고** — 예산표는 인상 후 단가로도 1회 계산. BYO = **4종 고정(2026-08-22 기준 하드코딩)**: `claude-fable-5` · `claude-opus-5` · `gpt-5.6-sol` · `gemini-3.1-pro`(현재 프리뷰 — GA 전환 시 ID 변동 여부 확인). 신모델 출시 시 수동 갱신. **커스텀 프로바이더 확장(2026-08-22)**: 4종은 기본 프리셋이지 허용 목록이 아니다 — 고급 경로에서 프로바이더·모델 직접 입력 허용(미결 7). 예상 비용 ~$0.3–0.8/실행, 체험 1,000명 ≈ 수십만 원 규모(검증 배터리 포함). 배터리 결과는 통과/주의/실패로만 표시 — 소수 재채점으로 정밀 수치를 주장하지 않는다. 원칙 유지(2026-08-20): **하네스 차등 없음** — 티어 차이는 구동 모델과 쿼터뿐. 남은 작업: **실행 1회의 호출 예산표**(루프·양방향 pairwise·챔피언 재채점·홀드아웃 라운드 0/종료·배터리·캘리브레이션 분리 계상 — **case_answering은 후보 1개 채점 = 가시 케이스 수 × 2콜(responder+grader)로 비용의 주범**: 10라운드 × 케이스 5개면 그것만 100콜, **가시 케이스 상한(예: 5) 결정 필요** — 상한은 템플릿 규격의 선언 필드로 승격(§6, 2026-08-22). 라운드별 케이스 샘플링으로 아끼는 것은 금지 — 곡선이 깨진다. **비용 구조 갱신(2026-08-22)**: expected 구조화(미결 1의 v0.4 백로그) 시 grader 콜의 상당 부분이 JS 결정적 검사로 대체되어 케이스당 2콜 → 1콜(executor)에 근접 — 예산표는 이 구조 기준으로 재계산)와 **수정→재검증 왕복의 쿼터 계정 규칙**(실행 쿼터와 별도의 재검증 상한 N회 권장 — 미포함이면 비용 상한이 뚫리고, 실행 쿼터에 포함이면 수정 두 번에 쿼터가 소진). 티어 구조 자체(프록시 유지 vs 생성 무료 + 실행 BYO 단일)는 **재논의 보류(2026-08-22)**.
3. LLM API 브라우저 CORS 벤더 검증 — **1주차 실측**. 사전 조사: Anthropic 공식 지원(전용 헤더), Gemini 지원, OpenAI 동작하나 비공식 → "experimental" 표기로 출시. 체험 경로는 서버 프록시 경유라 CORS 무관 — 실측 대상은 BYO 직행 3사 + 커스텀 프로바이더 대표 2종(OpenRouter, 로컬 Ollama — 2026-08-22 확장). 임의의 커스텀 endpoint는 CORS를 사전에 단정할 수 없으므로, 실측과 별개로 실행 전 연결 테스트가 제품 요건(미결 7).
4. **Evaluation Pack·루프 스펙 명세** (구 "루프 스펙·zip 구조") — 명문화할 것: 팩 구성(기준+gates, §3 원칙 4의 판정 절차 필드 전체, 홀드아웃 정책, 검증 리포트, 다이제스트), 채택 규칙 3종 + 예외 채택 모드 2종(결정적 전용·케이스 실측 중심 — §5.1.1), **게이트×채택의 실행 순서**(게이트 평가는 채택 판정 — pairwise 또는 스칼라 비교 — 진입 전. reject 후보는 진입 불가, cap 후보의 채택 시 의미 정의), 홀드아웃 불변식 2종과 채점 시점(라운드 0·종료 — §3 원칙 7), **auto 홀드아웃의 해시 결속**(승인 후 엔진이 생성하므로 definitionDigest가 아니라 팩 컴파일 시점의 팩 다이제스트가 케이스 해시를 잠근다 — 체리피킹 감사 가능성 + auto 케이스 수정 시 재승인 트리거의 기계적 정의), 홀드아웃 쓰기 = 재승인(재분할·배지 반납 폐기, 열람은 기록하지 않음), 면제 루프(결정적 전용)의 방어 세트 표기 규칙(리포트·캘리브레이션 "해당 없음" 표시 — 제3 채택 모드 루프의 pairwise "해당 없음" 표기도 여기서 함께 정의), 원샷 베이스라인(라운드 0)의 고급 모드 run.sh 포함, zip = 팩의 포장(§4.2), **케이스 실측 중심 루프의 제3 채택 모드 발동 조건·규칙**(§5.1.1, 2026-08-22 — 케이스 실측 가중치 기준 등 경계 정의), **케이스 트레이스의 명세**(포맷, 가시 케이스 한정 불변식, Generator 전달 방식 — §5.1.1). 홀드아웃 은닉 경계 결정(2026-08-20)은 유지: **경계는 사용자가 아니라 루프** — 클라이언트 배포 허용, 서버 채점·봉인 불필요, BYO 프라이버시·비용 제약 해소.
5. 워킹 스켈레톤 DoD — 특례 2건(§10)과 IndexedDB 체크포인트 반영.
6. 인수인계·온보딩 문서 템플릿 확정 및 데모 순위 (구 시험 요약본 실측을 대체 — 2026-08-22) — **1주차 원샷 델타 실측이 go/no-go** (프론트 투자 전 선행, §3 원칙 6). **데이터**: 공개 OSS 저장소 1곳의 이슈·디스커션 Q&A 30~40건, 시간 기준 분할(이전=가시, 최근=홀드아웃 — 실전과 같은 분포 이동). **비교 조건 4종**: ① 원샷 ② 동일 예산 "더 개선해줘" 반복 첨삭 ③ Harnest(동결 평가자 + 케이스 트레이스) ④ 동일 예산 best-of-N(§3 원칙 6 방어 카드). **성공 조건**: 홀드아웃 점수가 원샷·반복 첨삭 대비 상승, hard gate 위반 비증가, 가시 케이스 통째 암기 없음(분량 게이트로 검증). **실측에는 체험 티어 구성 그대로(`gemini-3.7-flash`·10회·빔 1) 1회를 필수 포함** — go/no-go 판정은 티어 구성 기준 델타로 한다(강한 모델로 통과한 델타가 실제 데모 경로에서 재현되지 않으면, 곡선 하락을 정직하게 표시하는 제품이 스스로 반증을 전시하게 된다). 델타가 안 나오면 도메인 전체를 버리기 전에 어느 케이스 유형에서 사라지는지 좁힌다. **해결 — go 판정(2026-08-22)**: hermes-agent 56케이스(가시 35/홀드아웃 21, 오염 프로브 17.0% 통과), 체험 티어 구성에서 성공 조건 4/4 충족 — ③루프 가시 77.1·홀드아웃 23.8, ①원샷(5,991자)·②반복 첨삭(6,773자)·④best-of-10(10회 전부) 분량 게이트 초과 실격. 반복 케이스 홀드아웃 기여 +8.3 vs 신규 +3.3. 전 과정(01 no-go → 02a 실격 → 02b go)과 정직한 해석은 experiments/delta-01/{PROTOCOL,report,report2}.md. **교훈 3건은 템플릿 규격(§6)에 반영**. 잔여: 트레이스 vs 실격 신호 기여 분리(델타 03, PROTOCOL §9.2).
7. API 키 온보딩 UX — BYO 모델 정책(2026-08-22, 커스텀 확장): **계층형 UX** — 기본 화면은 4종 프리셋(미결 2)에 키만 입력, 고급 접기에서 **커스텀 프로바이더 직접 입력**(OpenAI 호환 base URL + 모델 ID 자유 입력 — OpenRouter·로컬 Ollama 등을 어댑터 추가 없이 커버, OpenAI 어댑터의 base URL만 설정 가능하게). 비개발자는 커스텀 경로를 볼 일이 없어야 한다. 커스텀 endpoint는 **실행 전 연결 테스트**(1콜 ping)로 CORS·인증을 fail-fast — 루프 중간이 아니라 시작 전에 막히게. 모델 허용 목록은 두지 않는다 — **모델 선택의 품질은 허용 목록이 아니라 시험관 검증 배터리·캘리브레이션이 심사한다**(약한 저지 모델은 승인 게이트에서 통과/주의/실패로 드러난다). UX 순서 제약 주의: 저지·responder 모델은 판정 절차의 일부로 동결되므로(§3 원칙 4) **벤더·모델 선택은 승인 전에 확정**되어야 하고, 실행 시점에 해당 벤더 키가 없으면 루프를 시작할 수 없으며, 저지 모델 교체는 재승인이다(definitionDigest 변경). Generator 모델은 실험 쪽이라 이 제약 밖.
8. 관제실 UI 와이어프레임 구체화 (승인 화면의 수정→재검증 왕복 UX 포함 — §4.1)
9. 부가 기능 3종 상세 명세
10. 신청서 문안 v2 수정, 라이선스 요건 확인, 템플릿 owner 배정, 패키지 분리 계획
11. 시험관 검증의 입력 출처 — **결정** (2026-08-21): 큐레이션 꼼수 라이브러리(문헌 시딩) + 사용자 캘리브레이션이 프로브·ground truth 공급, 즉석 합성은 순서·변별력·안정성 입력으로 제한 (PHILOSOPHY.md §8).