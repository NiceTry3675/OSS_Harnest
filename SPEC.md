# Harnest 기획 스펙

> 오픈소스 개발 대회(osscontest.kr) · 교내 경진대회 출품 프로젝트
> 버전 1.1 (2026-08-19 — PHILOSOPHY.md 반영)
> 설계 결정의 근거와 철학은 [PHILOSOPHY.md](PHILOSOPHY.md) 참조.

---

## 1. 개요

### 1.1 한 줄 정의
**Harnest** — 사용자가 웹 인터뷰에 답하면, 사용자가 승인한 평가 기준(불가침)으로 산출물을 될 때까지 스스로 고치는 **자율 개선 루프**를 만들어주는 웹 서비스.

### 1.2 컨셉 문장
- **제품(대외)**: "AI에게 한 번 시키고 끝이 아니라, 당신이 승인한 기준으로 될 때까지 스스로 고치는 AI"
- **기술(심사)**: "에이전트는 이미 강하다. 없는 건 믿을 수 있는 채점표다"

### 1.3 이름
**Harnest** = harness + nest. nest는 실행 환경이 아니라 "에이전트가 벗어날 수 없는 평가 규칙의 울타리"를 의미.

---

## 2. 문제 정의와 포지셔닝

### 2.1 배경
Karpathy의 autoresearch 구조(수정 가능 파일 / 불가침 평가 스크립트 / 행동 지침 / 실행 루프, 지표 개선 시 유지·아니면 롤백)는 강력하지만, **하네스 설계 자체가 진입장벽**이다. 특히 "내 문제의 성공 기준을 어떻게 기계적으로 판정할 것인가"(채점표 설계)가 병목.

### 2.2 포지셔닝
- 공식 에이전트 하네스(Claude Code 등)는 "루프를 돌리는 손". Harnest는 그 손이 소비할 **스펙(불가침 평가자 + 수정 스코프 + 행동 지침)을 생성하는 위 계층** — harness-agnostic, 경쟁이 아닌 보완재/스택 관계.
- 연구 계열(Meta-Harness, AutoAgent 등)이 "하네스 반복 개선"을 다룰 때, Harnest는 **하네스 제로 생성 + HITL + 대중 웹 서비스**로 제품화.

### 2.3 타깃: 비개발자 퍼스트
- 주 타깃: 자소서·시간표·글쓰기 등 반복 개선이 필요한 일반 사용자. 웹에서 인터뷰 → 웹에서 실행 → 결과 확인까지 완결.
- 부 타깃(고급 모드): 개발자. 하네스 스펙 zip을 받아 로컬 에이전트 CLI로 실행.
- 언어 원칙: 사용자 화면에서 하네스/autoresearch/hill-climbing 용어 금지. HITL은 "채점 기준은 당신이 승인하고, AI는 그걸 못 건드린다"는 신뢰 장치로 표현.

---

## 3. 핵심 설계 원칙

1. **생성은 웹, 실행은 사용자 환경** — 서버는 생성(LLM 호출 + CRUD)만 담당. 실행은 비개발자=브라우저(Lite), 개발자=로컬(zip).
2. **HITL 두 지점만**:
   - 평가 기준 후보 제안 → 사용자 승인/수정
   - 생성된 루프 설정 최종 검토 → 실행 전 승인
   - 승인된 평가자는 아키텍처 수준에서 동결 = reward hacking 방어.
3. **템플릿 프리셋 + 커스텀 경로** — 폴더 1개 = 템플릿 1개, 커뮤니티 기여 가능 규격.
4. **평가 기준 원칙** — (A) 결정적 채점기, 또는 (B) 고정 루브릭 LLM-as-judge(동결) + 홀드아웃 케이스로 judge hacking 방어. 동결의 단위는 인원수가 아니라 **판정 절차**(루브릭 + 패널 구성 + 집계 규칙) — 내부 패널·투표는 허용, 루프에 반응한 변경은 금지.
5. **방어 세트 기본 탑재** — 홀드아웃 / grounding 기준 / pairwise 판정 / provenance 기록은 옵션이 아니라 기본값. 단, pairwise는 llm_judge 포함 루프에 한하고(결정적 채점기만 쓰는 루프엔 비용만 추가), grounding은 해당 조건에서 기본(`origin: "generated"` 등 — 인터뷰 스키마 §3). 결과 화면에 활성 방어를 명시.
6. **원샷 델타 시금석** — 모든 템플릿은 원샷 베이스라인(라운드 0) 대비 루프 개선 폭으로 자격을 심사. 델타가 없는 템플릿은 채택하지 않는다.
7. **강제는 루프에게, 통제는 사용자에게** — 봉인·동결·불가침은 루프에 대한 강제. 사용자의 열람은 자유이며 기록되지 않고, 결과에 영향을 주는 쓰기(기준 수정, 홀드아웃 변경, 방향 개입)만 provenance로 기록. 케이스가 풍부한 도메인은 홀드아웃 사용자 지정 지원(승인 시점에 지정 = HITL 지점 불변).

---

## 4. 제품 정의

### 4.1 사용자 흐름 (Lite 기본 경로)
1. 템플릿 선택 (예: 시험 요약본)
2. 인터뷰 위저드 — 답할 때마다 우측에 설계도가 실시간 조립 (라이브 블루프린트)
3. 평가 기준 승인 — 제안된 채점 기준과 **시험관 검증 리포트**(순서·변별력·안정성·꼼수 내성)를 확인·수정 후 승인, 승인 순간 잠금 연출
4. 루프 실행 — 라운드 0으로 원샷 베이스라인을 채점해 기준선으로 깔고, 브라우저에서 실험 반복. 관제실 화면(실험 트리 + 개선 곡선 + diff)으로 관찰
5. 결과 — 최종 산출물 + 개선 이력(원샷 대비 델타) + 홀드아웃 점수 + 활성 방어 세트 표시 + 공유 카드

### 4.2 실행 모드
| | Lite (기본) | 고급 모드 |
|---|---|---|
| 대상 | 비개발자, 텍스트 전용 도메인 | 개발자, 코드 실행 필요 도메인 |
| 실행 위치 | 브라우저 (루프 엔진 JS) | 로컬 (zip: 수정 대상 / 평가 스크립트 / program.md / run.sh) |
| LLM 호출 | 무료 체험: 서버 프록시(쿼터·저가 모델) / 이후: BYO API 키 브라우저 직행 | 사용자 로컬 환경의 에이전트 CLI |
| 결과 | 자동 기록·대시보드 | 결과 업로드 API로 대시보드 연동 |

하이브리드 원칙: **루프 엔진은 한 벌(브라우저)**, LLM 호출 경로만 스위치. BYO 경로는 키·민감 텍스트가 서버를 거치지 않음(프라이버시).

### 4.3 UX 아이덴티티: "챗봇이 아니라 관제실"
- 사용자는 AI와 대화하지 않고, AI가 실험하는 것을 **지켜보고 통제**한다.
- 실행 화면: 실험 트리(git 그래프 감성 — 채택 가지만 본선으로, 폐기 가지는 회색), 점수·개선 곡선 라이브 차트, 채택 시점별 diff 하이라이트.
- 위저드: 채팅 UI 금지. 폼 + 라이브 블루프린트.
- 불가침 연출: 평가 기준 승인 시 잠금(자물쇠) 애니메이션.

---

## 5. 기능 명세

### 5.1 핵심 기능
| 기능 | 설명 | 담당 파트 |
|---|---|---|
| 인터뷰 위저드 | 템플릿별 질문 세트, 라이브 블루프린트, 평가 기준 승인 UI | 프론트 |
| 생성 엔진 | 인터뷰 답변(JSON) → LLM 파이프라인 → 루프 스펙/zip 생성 + 시험관 검증 배터리(순서·변별력·안정성·꼼수 내성) | PM |
| 브라우저 루프 엔진 | 원샷 베이스라인(라운드 0) 채점, 멀티에이전트 hill-climbing 실행(하단 참조), 채점기 JS, LLM 호출 경로 스위치, 키 관리 | 프론트 |
| LLM 프록시 | 무료 체험용. 로그인 필수, 실행 쿼터 + 반복 상한 + 모델 제한 | 백엔드 |
| 결과 대시보드 | 실험 횟수, 채택/폐기 이력, 개선 곡선(원샷 베이스라인 기준선), 활성 방어 세트 표시, 로컬 실행 결과 업로드 | 프론트+백엔드 |
| 웹 백엔드 | 프로젝트 CRUD, 인증, 다운로드/공유, 결과 업로드 API | 백엔드 |

### 5.1.1 루프의 멀티에이전트 구조
직렬 hill-climbing 대신 역할 분리 + 병렬 탐색:

- **병렬 탐색 (beam)**: 라운드당 Generator 에이전트 N개가 서로 다른 방향의 변이를 동시 제안 → 전부 채점 → 최고만 채택. 국소 최적점 탈출 + 실험 트리가 실제로 갈라지는 시각 임팩트. 탐색 폭: 무료 체험 1 고정, BYO는 슬라이더(1~4).
- **Critic 에이전트**: 점수 정체(plateau) 감지 시 채점 내역·트레이스를 읽고 다음 변이 방향을 진단해 Generator에 전달. 근거: "점수만 피드백하면 개선 없음, 트레이스 공유 필요"라는 선행 연구 발견의 제품 번역.
- **채택은 pairwise, 서사는 스칼라**: 채택/롤백 판정은 챔피언 vs 후보의 pairwise 비교(위치 스왑, 장황함 보정)로 — 절대 점수보다 판정 신뢰도가 높다. 개선 곡선·홀드아웃 서사는 동결 루브릭의 스칼라 점수가 담당.
- **불가침 경계**: 실험하는 쪽(Generator/Critic)은 자유롭게 멀티에이전트. 채점하는 쪽(Judge)도 내부적으로 패널·투표 구성은 가능하나, **판정 절차(루브릭 + 패널 구성 + 집계 규칙) 전체가 승인 시점에 동결** — 루프에 반응해 변할 수 없다. 이 비대칭(실험은 가변, 판정은 불변)이 Harnest의 정체성.

### 5.2 채택 확정 부가 기능 (우선순위순)
1. **리플레이 모드** — 실행 기록 저장 → 타임라인 재생. 발표·심사 라이브 리스크 제거, 공유 기능의 기반.
2. **홀드아웃 점수 공개** — 숨겨둔 평가 케이스 점수를 결과 화면에 표시("본 적 없는 기준에서도 +N점"). judge hacking 방어를 신뢰 서사로 전환. (홀드아웃 채점 자체는 §3 원칙 5의 기본값 — 본 항목은 그 결과의 표시 UI만을 가리킴.) 카드·결과 화면에 홀드아웃 provenance(자동 분할 vs 사용자 지정) 표기.
3. **공유 카드** — 개선 곡선(예: 62→78점) 이미지/링크 내보내기. 유입 루프 + 정량 지표. 링크 프리뷰(OG 태그)는 백엔드 공유 엔드포인트에서 처리(§7 참조).

### 5.3 스트레치 골 (핵심 기능 완료 후 여유 시 추가)
- **루프 중간 개입(pause & steer)** — 루프를 멈추고 방향 힌트를 준 뒤 재개. 관제실 UX("지켜보고 통제한다")의 자연스러운 연장이라 여유 시 1순위. 단, 워킹 스켈레톤·부가 기능 3종보다 후순위.

### 5.4 로드맵 (대회 범위 제외)
- "스킬 최적화" 템플릿(SKILL.md 대상), GPU 필요 도메인(소형 ML 훈련 등)

---

## 6. 템플릿 라인업

모든 템플릿은 두 시금석을 통과해야 한다 — **원샷 델타**(원샷 베이스라인 대비 루프 개선 폭이 실증되는가)와 **기준의 저자**(평가 기준을 사용자만 쓸 수 있는가). 근거: PHILOSOPHY.md §2·§3.

### 비개발자 (본선 3~4개)
| 템플릿 | 평가 방식 | 상태 |
|---|---|---|
| 시험 요약본/치트시트 | 케이스=기출 — "요약본만으로 홀드아웃 기출을 푸는가" + 분량 제한 + grounding | **유력** — 데모 1순위 후보 (홀드아웃 서사 내장, PHILOSOPHY.md §4 가족 2) |
| 자기소개서 ↔ 채용공고 매칭 | 키워드 커버리지(결정적) + 고정 루브릭 | **확정** — "첨삭"이 아니라 특정 공고 매칭으로 포지셔닝 (버티컬 비교 회피) |
| 시간표/근무표 짜기 | 제약 위반 개수(완전 결정적) | **확정(조건부)** — 원샷 델타 검증 필요: 소규모 제약은 프론티어 LLM이 원샷으로 해결 |

예비: 설문지 설계, 면접 질문 세트, 팀 매뉴얼/FAQ, 개인 문체 가이드, 이메일 템플릿, 퀴즈/플래시카드. (기존 후보였던 SEO·유튜브 제목은 보편 기준 도메인이라 버티컬 비교에 노출 — 후순위.)

### 개발자 (고급 모드 1~2개)
- **프롬프트 최적화** (확정, 개발자 데모 겸용) / 예비: SQL 튜닝, 정규식·파서, 코드 성능.

### 템플릿 규격 (커뮤니티 기여 단위)
폴더 1개 = 질문 세트 + 채점기 + 루브릭 + 예제 + 홀드아웃 평가 케이스 + **꼼수 라이브러리**(이 도메인에서 관찰된 reward hack과 그 방어 — 시험관 검증의 프로브로 사용). 코드 없이도 기여 가능하게 설계.

경계 원칙: 템플릿은 신뢰 가능한 부품의 **범위 선언**(채점기 후보, 질문, 꼼수 라이브러리, 홀드아웃 생성 규칙), 엔진은 그 범위 안의 사용자 맞춤 **조합·파라미터화 + 검증**. "프리셋이 아니라 생성"이라는 방어 논리의 실체가 이 선이다. 템플릿은 **사용자 지정 홀드아웃 지원 여부**도 선언한다 — 케이스가 풍부한 도메인(기출 등)은 지원, 엔진이 케이스를 합성하는 도메인은 자동 분할만.

---

## 7. 아키텍처 요약

```
[인터뷰 위저드] --인터뷰 JSON--> [생성 엔진(서버)] --루프 스펙--> ┬ Lite: [브라우저 루프 엔진]
                                                              └ 고급: [zip 다운로드 → 로컬 실행]
[브라우저 루프 엔진] --LLM 호출--> 무료: 서버 프록시 / BYO: LLM API 직행
[실행 결과] --> [결과 대시보드(웹)]  (Lite 자동 / 로컬은 업로드)
```

- 서버는 임의 코드 실행 없음(생성 + 프록시 + CRUD만) → 저비용 지속 운영.
- 스택(확정): React+Vite+TS (SPA) / FastAPI + PostgreSQL / LLM API / Vercel+클라우드 / GitHub Actions / MIT or Apache 2.0.
  - SPA 선택 근거: 앱 핵심이 클라이언트 중심(브라우저 루프 엔진, 관제실, BYO 키 직행)이고 백엔드가 별도 계층이라 SSR 이점 없음. 루프 엔진 npm 패키지 분리(§8)와도 모노레포 구성이 자연스러움.
  - 단, 공유 링크의 OG 메타태그(링크 프리뷰)는 SPA가 처리 불가 → FastAPI가 공유 전용 엔드포인트에서 OG 태그 포함 HTML 응답 후 앱으로 리다이렉트 (백엔드 담당).
- 기술 검증 필요: BYO 직행용 LLM API의 브라우저 CORS 허용 여부 벤더별 확인.

---

## 8. 오픈소스 전략 (양방향)

- **받는 기여**: 폴더 단위 템플릿 규격 전면화 — 코드 못 짜는 사람도 기여하는 오픈소스. 템플릿 갤러리, 제출 템플릿 자동 검증 CI, good first issue 설계.
- **하는 기여**: 부품 2개를 독립 패키지로 분리 배포 — (1) 브라우저 hill-climbing 루프 엔진(npm), (2) 결정적 채점기 모음(키워드 커버리지, 제약 검사, 가독성 등).
- 서사: "Harnest는 서비스이자, 부품들이 각각 재사용 가능한 오픈소스 라이브러리."

---

## 9. 경쟁/차별 논리 (심사 대응)

| 비교 대상 | 그들 | Harnest |
|---|---|---|
| 공식 하네스 (Claude Code 등) | 루프를 돌리는 "손". 평가자 분리는 사용자가 지켜야 하는 규율 | 손이 소비할 "채점표"를 검증까지 마쳐 생성, 동결이 유일한 경로 — 스택 관계 |
| skill-creator eval | 매 라운드 사람 검토, 평가 기준에 벽 없음, 소비자=Claude | 규칙만 승인하면 무인 루프, 평가자 동결, 소비자=사람 |
| 연구 계열 (Meta-Harness 등) | 하네스 반복 개선 연구 | 하네스 제로 생성 + HITL 제품 |

방어 논리: 동결·홀드아웃 같은 개별 패턴은 어떤 하네스든 흉내 낼 수 있다 — 차별점은 기능이 아니라 (a) 동결할 가치가 있는 시험관을 검증해서 뽑아주는 생성 엔진, (b) 방어 세트(홀드아웃·grounding·pairwise·provenance)의 기본 탑재, (c) 실행에서 축적되는 도메인별 꼼수 라이브러리 — 기능은 베껴도 축적은 못 베낀다. 공식 진영이 eval 자동 생성을 내장해도 (d) 비개발자 도메인은 그들이 안 가는 영역, (e) 웹 인터뷰 진입로는 CLI와 다른 시장.

---

## 10. 팀 구성과 운영

| 역할 | 인원 | 담당 |
|---|---|---|
| PM + 생성 엔진 | 1 (준현) | 총괄, 인터뷰 JSON 스키마·생성물 구조 확정 |
| 프론트 | 2 | 인터뷰 위저드 + 브라우저 루프 엔진 + 관제실 대시보드 |
| 백엔드 | 1~2 | CRUD/인증 + LLM 프록시 + 결과 업로드 API |
| 템플릿 팩 | 1~2 | 도메인 owner 로테이션 (전원이 제품 전체 흐름 경험) |

운영 장치: **워킹 스켈레톤 우선** — 1~2주 내 "인터뷰(질문 3개) → 생성 → Lite 루프 1회전 → 결과 표시"를 조잡하게라도 전원이 함께 관통. 스켈레톤 템플릿은 **시간표**로 고정: 채점이 완전 결정적이라 LLM 저지 변수 없이 파이프라인을 디버깅할 수 있다 (제품 데모 순위와 별개인 테스트베드 가치). 계약 문서 3종(인터뷰 JSON 스키마 / 루프 스펙·zip 명세 / 스켈레톤 DoD)을 먼저 확정.

---

## 11. 성과 계획

1. 공개SW 오픈소스 개발자대회(osscontest.kr) 출품 (확정)
2. 교내 경진대회 발표 (확정)
3. 정식 서비스 배포 + 피드백 기반 개선 1회 이상
4. 오픈소스 커뮤니티: README·기여 가이드·템플릿 규격 완비, 외부 기여 유치, 부품 라이브러리 2종 배포
5. 기술 확산: 기술 블로그, 개발자 커뮤니티 공유
6. 정량 지표: 하네스 생성 건수, Lite 실행 수, 템플릿별 분포, 결과 업로드율, 공유 카드 유입

---

## 12. 미결 사항 (우선순위순)

1. 인터뷰 JSON 스키마 — **v0.2 완료** (2026-08-20): 시험관 검증 리포트 참조, 판정 절차 구성(패널·집계·pairwise → evaluation 블록으로 동결), 사용자 지정 홀드아웃(role: "cases" + caseList + holdout 정책), 위협 모델 문구 수정. 남은 것: 1차 제출 응답 스키마(기준 후보 + 검증 리포트 본문) — 스키마 문서 미결 4.
2. 무료 티어 정책 수치 (실행 횟수, 반복 상한, 모델, 남용 방어 — **시험관 검증 배터리 비용 포함**: 재채점 안정성 검사 등 LLM 호출 다회). 원칙 결정됨(2026-08-20): **하네스 차등 없음** — 무료 체험도 방어 세트·검증 배터리는 동일하게 돌고, 티어 차이는 구동 모델과 쿼터뿐.
3. LLM API 브라우저 CORS 벤더 검증
4. 루프 스펙·zip 구조 명세 (Lite/고급 이원화, pairwise 채택 판정 명세 포함). 홀드아웃 은닉은 결정됨(2026-08-20): **은닉 경계는 사용자가 아니라 루프** — 홀드아웃은 클라이언트에 내려가도 되며, 불변식은 "Generator/Critic 컨텍스트에 절대 포함되지 않는다"(루프 엔진이 오픈소스라 코드로 검증 가능). 사용자 열람은 자기기만 문제라 방어하지 않고 UI 비표시로만 처리 → 서버 채점·봉인 불필요, BYO 프라이버시·비용 제약 해소. 남은 작업: 이 불변식의 명문화 + 원샷 베이스라인(라운드 0)의 고급 모드 run.sh 포함 명세 + 홀드아웃 쓰기 회계(케이스 수정·가시 세트 이동 시 재분할 우선, 불가하면 배지 반납 — 열람은 기록하지 않음) 명세.
5. 워킹 스켈레톤 DoD
6. 시험 요약본 템플릿 확정 및 데모 순위 (보류 중 — 확정은 회의가 아니라 **원샷 델타 실측 후**: 프로토타입 델타 측정이 선행, §3 원칙 6)
7. API 키 온보딩 UX
8. 관제실 UI 와이어프레임 구체화
9. 부가 기능 3종 상세 명세
10. 신청서 문안 v2 수정, 라이선스 요건 확인, 템플릿 owner 배정, 패키지 분리 계획
11. 시험관 검증의 입력 출처 — 합성 변형본 vs 실전 꼼수 라이브러리 (PHILOSOPHY.md §8)