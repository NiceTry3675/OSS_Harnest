{"text": "# Hermes Agent: Onboarding & Frequently Asked Questions (FAQ)\n\nWelcome to **Hermes Agent**! This guide compiles essential setup steps, architectural patterns, known bug resolutions, and configuration solutions.\n\n---\n\n## 1. Installation, Platform & Environment Support\n\n### What platforms are natively supported?\n- **Linux & macOS**: Supported natively via the official install script (`curl -fsSL ... | bash`).\n- **Windows**: Windows 10/11 is a **Tier 1 supported platform** via native PowerShell (`install.ps1`). Running Hermes does not require WSL2; WSL2 is only required if using the Web Dashboard's embedded POSIX PTY terminal pane.\n- **Docker & NAS**: Multi-arch Docker images support `linux/amd64` and `linux/arm64` (Apple Silicon, Raspberry Pi). For NAS deployments (UGOS Pro, Synology, unRAID) encountering `[Errno 13] Permission denied` on `/opt/data/config.yaml`, pass `PUID` and `PGID` (or `HERMES_UID`/`HERMES_GID`) environment variables to remap container user permissions.\n- **Android / Termux**: Supported natively. The installer detects Termux, configures `ANDROID_API_LEVEL`, installs curated `termux-all` dependencies, and points temporary filesystem operations to a writable `TMPDIR`.\n\n### Why does `hermes update` stall during Node.js dependency installation?\nThe Node.js update step installs packages like Camofox, which silently downloads browser binaries in the background during postinstall without printing live terminal progress.\n\n---\n\n## 2. Models, Providers & API Configuration\n\n### How do I configure local models (Ollama, LocalAI)?\n- **Preventing Cloud Auto-Routing**: If `provider: auto` is set, Hermes scans your environment variables and may route to cloud APIs if keys such as `ANTHROPIC_API_KEY` are present. Explicitly set `provider: custom`:\n  ```yaml\n  model:\n    default: ollama/<model-name>\n    provider: custom\n    base_url: http://localhost:11434/v1\n  fallback_providers: []\n  ```\n- **Performance Optimization for Small Models (4B\u20139B)**: Small local models can respond slowly because Hermes sends ~45 tool schemas per turn. To optimize speed:\n  - Disable unneeded toolsets using `hermes tools`.\n  - Use faster quantizations or 32B+ parameter models for tool-heavy workflows.\n  - Set `terminal.backend: local` to avoid container overhead.\n- **Token Tracking & Usage Statistics**: In earlier releases (such as v0.14.0), `/usage` showing 0 tokens and `Cost status: unknown` against OpenAI-compatible local providers (e.g., LocalAI) was a known bug caused by missing `stream_options` and `extra_body` forwarding. This required a code fix on `main` where Hermes now automatically sends `stream_options={\"include_usage\": true}` on OpenAI-compatible streaming completions. Update via `hermes update` to resolve.\n- **Vision Support**: For custom OpenAI-compatible or Ollama vision endpoints, set `supports_vision: true` in your model configuration.\n\n### How do other popular providers connect?\n- **Ollama Cloud**: Fixed in v0.15.0 (`v2026.5.28`). Built-in `ollama-cloud` provider routes directly to OpenAI-compatible `https://ollama.com/v1`, avoiding older `/api` 404 errors.\n- **DeepSeek V4 Charges / Model Normalization**: `_normalize_for_deepseek()` historically forced unrecognized models like `deepseek-v4-flash` to `deepseek-chat` (which upstream mapped to expensive `deepseek-v4-pro`). Update to current releases where `deepseek-v4-flash` and `deepseek-v4-pro` pass through without normalization.\n- **Azure AI Foundry**: Supported with auto-detection. Set `provider: azure-foundry` or run `hermes setup` to auto-detect `ANTHROPIC_FOUNDRY_BASE_URL`.\n- **Anthropic / Claude Code**: Providing a Claude API key bills under API token rates. To use Pro/Max subscription allowances, authenticate via OAuth instead of an API key.\n- **Google Gemini**: Legacy CLI/antigravity OAuth flows were removed due to upstream restrictions. Authenticate using Google AI Studio keys (`GOOGLE_API_KEY` / `GEMINI_API_KEY`) or Vertex AI.\n\n### Does Hermes support automatic smart model routing?\n**No.** By maintainer policy, Hermes does not support per-turn complexity routing, cheap/strong tier switching, or automatic model selection. Model selection stays locked to the session's primary model. For sub-agent delegation, configure `delegation.provider` / `delegation.model` in `config.yaml`, or switch manually using `/model <name>`.\n\n---\n\n## 3. Agent Core, Memory & Context Management\n\n### What is the difference between `SOUL.md`, `USER.md`, and `MEMORY.md`?\n- **`SOUL.md`**: Stores agent persona, identity, and backstory. (Installer empty template scaffold issue fixed in `v2026.7.1` to seed real defaults).\n- **`USER.md`**: Stores persistent user identity, preferences, and profile information (targeted with memory tool `target=\"user\"`).\n- **`MEMORY.md`**: Stores general persistent facts and conversational knowledge.\n\n### How do I configure local Honcho memory?\nIn release `v2026.3.30`+, activation gates accept `base_url` without requiring a remote key and auto-inject a `\"local\"` placeholder API key when connecting to `localhost`, `127.0.0.1`, or `::1`. Simply upgrade via `hermes update`.\n\n### Can I contribute a new in-tree memory provider?\n`plugins/memory/` is **closed to new in-tree additions**. Publish custom memory backends as standalone plugins implementing the `MemoryProvider` ABC (`sync_turn`, `prefetch`, `shutdown`) in `~/.hermes/plugins/` or via pip entry points.\n\n### How does Hermes handle long contexts and limits?\n- **Context Pruning**: `ContextCompressor` (and the pluggable `ContextEngine` ABC) deduplicates older tool outputs while retaining recent context.\n- **Turn Limits**: `agent.max_turns` defaults to 90 (override via `hermes chat --max-turns <N>`).\n- **Autonomous Tasks**: Run `/goal <task>` to initiate a persistent Ralph loop across turns. Paused budgets can be resumed with `/goal resume`.\n\n---\n\n## 4. Tools, Skills, Observability & API Server\n\n### How do I add custom tools and track skills?\n- **Custom Tools**: Create a plugin in `~/.hermes/plugins/<name>` using `ctx.register_tool(...)`, enable it with `hermes plugins enable <name>`, and ensure it is assigned to an enabled toolset.\n- **Skill Telemetry**: Tracked in `~/.hermes/skills/.usage.json`. View stats via `/insights` or `hermes insights`.\n- **Tracing**: Enable full tool-call tracing via bundled Langfuse: `hermes plugins enable observability/langfuse`.\n- **MCP Media**: Base64 `ImageContent` from MCP tools is automatically converted to `MEDIA:<path>` tags and forwarded to messaging platforms.\n- **Open WebUI Streaming**: The bug where intermediate tool progress events failed to reach Open WebUI due to a `_on_tool_progress` callback argument mismatch was resolved in `v2026.4.8`.\n\n---\n\n## 5. Gateway, Messaging Platforms & Web UI\n\n### How do I launch the Web Dashboard?\nRun `hermes dashboard` to launch the local interface at `http://127.0.0.1:9119`, complete with a browser-based TUI chat.\n\n### How do I set up and authorize Telegram & Discord?\nRun `hermes gateway setup` to launch the interactive platform wizard.\n- **Telegram Authorization**: `allowed_chats` is a group-chat filter only. For DMs, configure user authorization in `~/.hermes/config.yaml`:\n  ```yaml\n  telegram:\n    allow_from: \"YOUR_TELEGRAM_USER_ID\"  # Sets TELEGRAM_ALLOWED_USERS\n  ```\n  Or approve via pairing code (`hermes pairing approve telegram <CODE>`), or set `telegram.allow_all: true`.\n- **Telegram Session Resets**: As of `v2026.5.7`, crash/restart handling preserves conversation history via `resume_pending` rather than wiping sessions. Unconfigured `session_reset` defaults to `mode: none`.\n\n### How do file attachments work across gateways?\n- **Inbound**: Uploaded code and text files (`.py`, `.txt`) are automatically injected into the agent prompt context.\n- **Outbound**: Agent-generated files are sent natively via platform adapters (SMTP email attachments, Matrix `m.file` uploads, DingTalk Markdown `![image](url)`).\n\n### Multi-Agent Personas & Multi-Profile Gateways\n- **Per-Channel Persona**: Set `channel_prompts` per channel ID for Discord, Telegram, Slack, or Mattermost.\n- **Multiplexed Gateway**: A single gateway process can host multiple isolated profile accounts (e.g., separate WeChat/Weixin or Telegram bots) with scoped credentials and sessions.\n- **Subagents**: Use the `delegate_task` tool to spin up concurrent isolated child agents.\n\n---\n\n## 6. Security Vulnerability Reporting\n\nReport security issues via **GitHub Security Advisories** or email **security@nousresearch.com** per `SECURITY.md`. Do not post vulnerability details in public issues.", "tag": "h9", "model": "gemini-3.7-flash"}