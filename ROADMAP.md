# 로드맵

이 문서는 아직 구현하지 않았거나 의도적으로 보류한 일만 다룬다. 완료 이력은 Git 기록과
[`experiments/`](experiments/README.md)에 남긴다.

## 다음 범위

- **핵심 UX 다듬기** — 구현된 수정→재검증 왕복과 결과 화면을 사용자 테스트로 확인하고,
  확인된 문제만 고친다.

- **용어 통일 2차** — 1차 프런트 적용(2026-08-26)에서 남긴 입력 화면·AI 연결·저장 버튼·종료
  사유 문구와 핵심 제품 문서의 용어를 [`docs/terminology-inventory.md`](docs/terminology-inventory.md)
  §2의 대표 용어 기준으로 정리한다.

## 보류

- **호스티드 무료 체험 프록시** — 운영 비용과 쿼터 정책을 결정하기 전에는 구현하지 않는다.
- **pairwise 채택과 빔 탐색** — 현재 스칼라·라운드당 후보 1개 경로의 필수 범위가 아니며, 기본 흐름이
  안정된 뒤 확장한다.
- **`case_execution` 일반화** — 구조화된 기대값과 로그→케이스 확인 위저드는 후속 계약
  버전에서 다룬다.
- **npm 패키지 배포** — 계약이 안정되기 전에는 배포하지 않는다. 배포 시
  `@harnest/contracts`와 `@harnest/loop-engine`부터 검토한다.
- **외부 기록 가져오기·진행 재개** — 현재 JSON 봉투는 완료 기록의 감사·보관용이다. 편집 상태와
  재검증 계수까지 이식하는 가져오기·재개는 별도 디코더와 마이그레이션 계약 뒤에 다룬다.
- **스캔 PDF OCR** — 첨부 추출은 PDF의 텍스트 레이어만 읽는다. 이미지 스캔 PDF의 OCR은
  클라이언트 측 비용·의존성을 검토하기 전에는 구현하지 않는다.
- **케이스 초안 무문서 프로브 필터** — 초안 케이스를 responder가 문서 없이 풀어 보게 해
  너무 쉬운 케이스를 걸러내는 자동 필터. 멀티홉 초안(`experiments/multihop-01`)이 무문서
  정답률을 낮추지만 delta-01 게이트 기준 "주의" 구간에 남아, 보조 호출 예산 정책을 정한 뒤
  프로브 필터 도입을 재검토한다.

## 현재 범위 밖

- **커스텀 endpoint** — OpenAI 호환 임의 base URL 입력. Ollama 로컬 주소는 받지만 그 외
  임의 endpoint는 받지 않는다.
- **제품 확장** — 추가 템플릿과 부가 기능
